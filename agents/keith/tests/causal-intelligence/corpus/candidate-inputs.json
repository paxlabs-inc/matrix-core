{
  "schema_version": 1,
  "purpose": "development-only proposer and lesson input",
  "allowed_files": [
    "development.json"
  ],
  "external_fixture_access": false,
  "held_out_feedback": "aggregate operator decision only; no per-case outputs, query text, or labels",
  "candidate_workspace": "fresh export containing only allowed_files; repository root and evaluator root must not be mounted"
}
