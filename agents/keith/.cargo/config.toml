[alias]
xtask = "run --quiet -p keith-xtask --"
ci = "run --quiet -p keith-xtask -- ci"
clean-checkout = "run --quiet -p keith-xtask -- clean-checkout"
dependency-policy = "run --quiet -p keith-xtask -- dependency-policy"
platform-gate = "run --quiet -p keith-xtask -- platform-gate"
security-gate = "run --quiet -p keith-xtask -- security-gate"
