# React Router
.react-router
build/
public/build/

# Wrangler & Cloudflare
.wrangler
wrangler.toml.local
.env
.env.local
vars.json
# Local-only Worker vars. NEVER commit: even dev values normalize the habit
# of putting secrets in tracked files. Template lives at .dev.vars.example.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Node modules & dependencies
node_modules/
.npm
.yarn
.pnp*
.yarn-integrity

# Vite
dist/
dist-ssr/
*.local

# OS & Environment Files
.DS_Store
Thumbs.db

# TypeScript / Logs
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
error.log

