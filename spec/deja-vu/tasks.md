<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: DEJA-VU — provenance + detection first, then retrieval + feed, then the lexical index, then backfill/failsafe/surface, then the end-to-end proof

## Tasks

## P0 — Foundation: provenance and detection

- [x] 1. Wave 1 — memories learn where they came from; Neo learns to notice
  - [x] 1.1 derived_from provenance edges at the consolidator write site
    - neo/internal/writeback/consolidator.go + neo/internal/memory pager write surface: thread the conversation id + processed seq range into the consolidation write path; on every persisted memory, AddEdge derived_from -> matrix://cortex/session/<conv>/<seq> refs (best-effort, never fails the memory write)
    - cortex-side: session-ref URI parse/format helpers if missing; resolver ExpandToTranscript(memoryURI, radius) following derived_from edges -> Transcript slice with provenance metadata {conv, date, seqLo, seqHi, exact}; fallback ladder (rollup window SeqLo/SeqHi + CreatedAt proximity) marked approximate
    - Tests on real cortex: consolidator run (httptest SSE LLM seam) writes memory + edge; resolver round-trips to the exact verbatim slice; edge failure does not fail the write; fallback path resolves an edge-less memory approximately
    - _Requirements: 1.1, 1.2, 1.3_
  - [x] 1.2 Trigger lexicon + cheap-lane referent extraction
    - neo/internal/agent (new episodic.go): compiled deterministic trigger lexicon (remembrance verbs, shared-past deixis, temporal deixis; negative guards like 'remember to'); classify(userMsg) -> {fired, class}; skipped for wake markers/guidance/non-user turns
    - Cheap-lane extraction (consolidation model client): one deadline-bounded call -> {referent, timeHint, scopeHint}; parse timeHint to a time window; fail-open fallback = raw message as referent, default horizon
    - Tests: table tests over the lexicon incl. negatives; extraction against httptest SSE LLM incl. timeout fallback; deadline bound proven
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

## P0 — Retrieval and the feed

- [x] 2. Wave 2 — find the exact thing, make it resident
  - [x] 2.1 Episodic retrieval pipeline (lanes 1+2 + current-conversation lane)
    - neo/internal/memory (pager surface EpisodicRetrieve(referent, timeWindow, budget)): lane 1 semantic Find over memories (reuse Retrieve seed lanes, time-filtered); lane 2 expand top hits via the task 1.1 resolver to verbatim cross-conversation excerpts; current-conversation lane via neo/internal/recall over older-than-T2 turns; dedup, rank, budget caps (tokens, k, wall-clock deadline); empty-on-failure
    - Tests on real cortex + real conversation store: cross-conversation exact excerpt retrieval, time-window filtering, budget enforcement, fail-open under broken embedder
    - _Requirements: 3.1, 3.2, 3.4, 3.5_
  - [x] 2.2 Episodic block on the cmTail + turn signal + observability event
    - neo/internal/agent/continuous.go: generalize the relevance-push site — prepareTurn runs episodic detection; on fire, run EpisodicRetrieve and append the rendered episodic block (header + excerpts w/ provenance + grounding instruction) to cmTail; token cap independent of Activate trim; surfaced-set dedup on excerpt refs; set turn.episodicPending, clear on surfaced grounding or memory_recall dispatch
    - Extend the memory.activation broker event with the episodic excerpt fields + trigger class; keep traceWorkspaceTypes persistence; internal metrics line (trigger, lanes, hits, tokens, latency)
    - Tests on the real Chat loop (httptest SSE LLM): E2E remember-when turn -> block present in the assembled window pre-generation with correct provenance; turnSeq==1 push unchanged; no-trigger turns byte-unchanged; empty pipeline -> no block + episodicPending set; window-law invariants (activationAssemblies once/turn, windowAssemblies once/step) hold
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 8.1, 8.3, 9.1_

## P1 — Lexical reach

- [x] 3. Wave 3 — exact words, replay-safe
  - [x] 3.1 Derived BM25 lexical index over sess/
    - cortex (new lexindex or store-adjacent package): deterministic tokenizer + BM25-style postings under a dedicated derived key family; incremental build at AppendMessage; delete+rebuild from journal/sess records byte-deterministic; values through the existing seal seam; query(term/phrase, timeRange, k) -> scored (conv, seq) hits; NO SMT writes
    - Tests on real Pebble: incremental vs rebuilt postings byte-identical; OverallRoot + anchored roots byte-identical index-on vs index-off vs delete-then-rebuild; replay byte-identity unchanged; sealed-value round-trip; corrupted-index fail-open (query returns empty)
    - _Requirements: 5.1, 5.2, 5.3_
  - [x] 3.2 Hybrid fusion into the pipeline and reactive recall
    - Wire lane 3 into EpisodicRetrieve with reciprocal rank fusion across lanes; wire the fused hybrid into pager.Recall / memory_recall for transcript-reaching queries (recallRecursive gains the lexical lane; rendering carries provenance)
    - Tests: exact-word query that semantic recall misses is found via the lexical lane in BOTH the automatic push and memory_recall; fusion ordering deterministic; budgets hold
    - _Requirements: 3.3, 5.4_

## P1 — History, failsafe, and the visible surface

- [x] 4. Wave 4 — backfill, Cassandra net, client legibility
  - [x] 4.1 Chronos idle backfill sweep
    - Chronos-scheduled sweep (reuse busy-check/reschedule/jitter conventions): walk historical sess/ building postings + heuristically backfill derived_from edges for pre-existing memories (actor + CreatedAt + conversation containment; heuristic edges marked distinguishably); checkpointed derived record, batch-capped per wake, busy-defer
    - Tests: resumable after simulated restart, idempotent (second full run = no-op, proven), busy-defer honored, heuristic vs exact edge marking
    - _Requirements: 7.1, 7.2, 7.3_
  - [x] 4.2 Cassandra trigEpisodicUngrounded
    - neo/internal/agent/cassandra.go + signals.go: one new stepSignals field (episodicPending); new doubt trigger + template (check memory_recall before asserting the past); classify placement respecting existing precedence; cooldown/caps unchanged
    - Tests: fires exactly on (detected AND ungrounded AND closing), never otherwise; non-episodic turn trigger behavior byte-unchanged (regression sweep over existing cassandra tests); dual-record + content-only guardrails hold
    - _Requirements: 6.1, 6.2, 6.3, 9.3_
  - [x] 4.3 Client surface: recalled excerpts in the timeline
    - apps/client: render episodic memory.activation excerpts in the existing memory/timeline workspace surface (conversation/date/exactness shown); parse in useChat + buildTaskFromTrace so it survives reopen; i18n x5 locales; house rules
    - Verify: tsc + eslint + vitest green on changed files
    - _Requirements: 8.1, 8.2_
  - [x] 4.4 Timeline reads Neo's learned-memory actor
    - Route the client Timeline read endpoints through Neo's live Pager instead of proxying them to the separate executor Cortex actor; preserve recent, type-count, type-filter, semantic-search, and as-of behavior
    - Regression test on a real Pebble Cortex proves learned Fact and Preference memories are returned from /memory/recent and remain filterable through /memory/search
    - _Requirements: 8.2_

## P2 — Proof

- [x] 5. Wave 5 — the whole loop, adversarially
  - [x] 5.1 DEJA-VU property + E2E suite
    - **Property DEJA-VU: on real components end-to-end — memory consolidated from conversation A, 'remember when' in conversation B, verbatim A-excerpt resident in B's window pre-generation with correct provenance; hybrid lexical+semantic finds exact words semantic-only misses; cortex integrity untouched (OverallRoot/anchored-root identity across index on/off/rebuild, replay identity); deja-vu fully fail-open (broken embedder, dead cheap lane, corrupted index -> normal turn, no block, no error); Cassandra failsafe fires iff detected-ungrounded-closing; backfilled history retrievable — no fakes anywhere**
    - **Validates: Requirements 9.1, 9.2, 9.3, 9.4**

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["2.1", "2.2"] },
    { "id": 3,  "tasks": ["3.1", "3.2"] },
    { "id": 4,  "tasks": ["4.1", "4.2", "4.3", "4.4"] },
    { "id": 5,  "tasks": ["5.1"] }
  ]
}
```
