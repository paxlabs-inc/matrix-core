<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: BROWSER-FILMSTRIP — Neo's Computer live-browsing filmstrip

## Tasks

## P0 — Foundations: per-user Playwright and the media persist seam

- [x] 1. Unlock local capture and stop discarding bytes
  - [x] 1.1 Bake a per-user Playwright into the Railway daemon image; point MATRIX_BROWSER_URL local
    - Install Playwright + its browser binaries into the daemon deploy image so a fresh per-user Railway container (24 vCPU / 48 GB) can navigate and screenshot locally; run the per-user Playwright MCP in-container and set MATRIX_BROWSER_URL to the local instance (replacing the shared Fly matrix-browser.flycast endpoint)
    - Keep the existing browser MCP bridge (tools/browser/browser.mjs) unchanged in wire shape — initialize/tools/list answered locally, tools/call forwarded to the local instance
    - Verify: a container built from the image navigates to a URL and takes a screenshot locally with no Fly/shared endpoint reachable
    - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - [x] 1.2 Persist image content blocks to the media plane; keep the model transcript terse
    - In summarizeNonText (neo/internal/tools/tools.go:1010-1031), when a content block is an image, persist c.Data to MediaDir (reuse mintMediaID + write from neo/internal/server/media.go:121-231) and produce a /media/<id> URL; carry the URL out-of-band on a new ToolEvent field (agent.go:72-79) so the model-facing Result string stays a terse placeholder
    - Keep persistence best-effort: a media write error returns the placeholder and never fails the tool call or turn
    - Test: a real image content block round-trips to a served /media/<id> asset AND the model-facing result string contains neither the bytes nor the URL
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

## P1 — Capture on navigation and surface it on the browser step

- [x] 2. Turn navigations into durable frames
  - [x] 2.1 Auto-capture a viewport JPEG on each view-changing browser action
    - In the browser tool dispatch, after a successful view-changing action (browser_navigate, browser_navigate_back, browser_click, form submit), issue a follow-up browser_take_screenshot (viewport, JPEG, quality/resolution bounded) on the same MCP session and run it through the task 1.2 persist path
    - Make it deterministic and model-invisible (no tool call added to the model transcript, no tokens); bound it with a per-run cap and gate behind NEO_BROWSER_AUTOSHOT (default on); keep it best-effort (a screenshot failure never blocks the navigation or turn)
    - Test: a real navigation produces a persisted frame; disabling the flag suppresses capture; the per-run cap holds
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - [x] 2.2 Enrich the browser tool.step with action/url/page_title/screenshot_url and confirm replay
    - In surfaceTool (neo/internal/server/engine.go:753-810) populate the browser tool.step fields with action, url, page_title, and screenshot_url (from the ToolEvent), reusing the already-whitelisted tool.step type so no traceWorkspaceTypes change is needed
    - Confirm the persisted event carries only the /media URL (never inline bytes) and that handleTrace replay (server.go:299-319) rebuilds the browser steps on reopen with their screenshot_urls
    - Test: after reopen the browser steps replay with their screenshot_urls and the referenced /media assets still serve
    - _Requirements: 4.1, 4.2, 4.3, 4.4_

## P2 — Render the filmstrip in Neo's Computer

- [x] 3. Make it feel live on the client
  - [x] 3.1 Authed still loading + filmstrip via the existing browser frame and scrubber
    - Switch BrowserView's screenshot render (neo-workspace.tsx:333-341) from an unauthed <img> to the authed loadMediaObjectURL blob pattern (apps/client/lib/api/media.ts:65-74), revoking the object URL on unmount
    - Confirm each navigation is a distinct browser step id so buildScreens (neo-computer.tsx:116-222) accumulates them as sibling screens (not upsert-overwrite, useChat.ts:284-292); label frames 'Neo viewed · <ts>', never a live-video claim; obey house rules (background-contrast separation, no border-for-depth, no glow/emoji)
    - Test (RTL): a sequence of browser steps renders as an ordered, scrubbable, crossfading filmstrip with the Live pill; stills load via the authed loader
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

## P3 — Guardrails, optional polish, and the end-to-end proof

- [x] 4. Bound it, polish it, prove it
  - [x] 4.1 Cost/honesty guardrails and bounded media growth
    - Enforce viewport-JPEG, quality/resolution bounds, and the per-run cap on capture; add a retention/GC sweep that bounds screenshot volume on /data/media (parallel to the trace retain cap)
    - Confirm no frame is presented as anything other than a faithful capture at its timestamp
    - Test: capture format/size bounds hold; the GC sweep removes stills past retention
    - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - [x] 4.2 Optional v2 polish: dedicated filmstrip history strip
    - Add a horizontal thumbnail history strip of the session's stills inside the browser view, in order, with click-to-scrub and still-to-still crossfade; additive only (v1 correctness does not depend on it); same house rules
    - Test: the strip lists prior stills in order and selecting one shows it
    - _Requirements: 6.1, 6.2, 6.3_
  - [x] 4.3 No-fakes end-to-end proof of the live-browsing filmstrip
    - **Property BROWSER-FILMSTRIP: on a REAL navigation sequence each nav produces a persisted, servable frame while the model transcript stays free of bytes/URL; after reopen the browser steps replay with their screenshot_urls and the filmstrip rebuilds with /media assets still serving — real screenshot, real media persistence/serving, real tool.step trace-replay, real client reducer, no fakes**
    - **Validates: Requirements 8.1, 8.2, 8.3**

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["2.1", "2.2"] },
    { "id": 3,  "tasks": ["3.1"] },
    { "id": 4,  "tasks": ["4.1", "4.2"] },
    { "id": 5,  "tasks": ["4.3"] }
  ]
}
```
