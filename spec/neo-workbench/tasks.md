<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: NEO-WORKBENCH — Neo takes over coding behind one bolt.diy-shaped workbench

## Tasks

## P0 — Daemon foundations: workspace API, event enrichment, projects

- [x] 1. Give the workbench a real backend on the Neo daemon
  - [x] 1.1 Workspace API: project-scoped tree/read/WRITE/diff/exec with one escape-rejecting path seam
    - Add project-scoped workspace endpoints to the Neo daemon (neo/internal/server): file tree, file read, atomic file write returning a version/hash, diff, and bounded exec — auth-gated like /media
    - Normalize every path through a single seam that rejects workspace escapes (port the cody edit.Rel posture); root = the project subdirectory under the VM workspace root
    - Test: read/write/diff/exec round-trip against a real workspace dir; escape attempts rejected; concurrent write returns detectable staleness via the version/hash
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [x] 1.2 Event enrichment: per-action status transitions + bounded live-typing deltas on editor/terminal steps
    - Emit real per-step status transitions (running → done/failed) on editor/terminal tool.steps from the dispatch seam (ToolEvent out-of-band pattern proven by browser-filmstrip: agent.go ToolEvent → engine.go surfaceTool → traceWorkspaceTypes durability)
    - Stream bounded file-content deltas while a file write is in flight (size/rate capped, best-effort — a dropped stream degrades to the final step content); final persisted step carries complete state
    - Test: a real file-write dispatch emits running→done with progressive deltas converging byte-identical to disk; trace replay carries final statuses only
    - _Requirements: 3.2, 6.1, 6.3_
  - [x] 1.3 Projects registry on the Neo daemon + project-tagged conversations
    - Implement the minimal project registry (list/create/rename/archive/delete over workspace subdirectories) on the Neo daemon, replacing codyd's project routes
    - Tag conversations with a project id so the conversation list filters per project (server-backed history, replacing the localStorage recent-runs stopgap)
    - Test: real registry round-trip; conversation list filtered by project against real stored conversations
    - _Requirements: 8.1, 1.3_

## P0 — Client shell: one workbench, Neo identity

- [x] 2. Collapse the tiers into one bolt-shaped shell
  - [x] 2.1 One workbench shell: slide-in panel + Code/Diff/Preview slider + terminal toggle, rebranded to Neo
    - Replace the tier dispatcher (cody-workspace.tsx) with ONE workbench layout: chat rail primary, workbench slides in from the right on work arrival (manual toggle too, full-width under 1024px), three-position Code/Diff/Preview slider over mounted panels, terminal panel toggle
    - House rules on the existing token ladder (bg-surface-* separation, single accent, no border strokes/glow/emoji/purple); fix neo-workspace.tsx's border-zinc violations to surface tokens; rebrand shell copy to Neo across all 5 locales
    - Test (RTL): slider switching preserves panel state; workbench opens on first work step; no Cody-branded strings render
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 1.4, 8.2_
  - [x] 2.2 Chat rail = real Neo conversation over useChat; server-backed per-project history
    - Drive the coding route's chat rail with the existing useChat reducer over the Neo daemon base path (live + buildTaskFromTrace reopen); retire useCody from the live surface
    - History page lists Neo conversations filtered to the active project (task 1.3 tagging); delete the recent-runs localStorage layer from the live path
    - Test: the coding chat rail folds a real Neo event trace into the same state the dashboard reducer produces; history renders from the real conversation list
    - _Requirements: 1.1, 1.2, 1.3, 1.5_

## P1 — The workbench panes

- [x] 3. Code, diff, terminal, artifact cards, live typing
  - [x] 3.1 Code view: tree + editable CodeMirror editor with save, unsaved dots, and the conflict posture
    - Compose CodyTree (react-arborist) with a new CodeMirror 6 editor pane (language packs by extension); open/save through the task 1.1 workspace API; dirty buffers show unsaved dots on tree + tab
    - Conflict posture: Neo-writing files render read-only while in flight; a Neo write over an unsaved user buffer surfaces a diff with explicit take-Neo's/keep-mine — never a silent clobber (use the write API's version/hash staleness signal)
    - Test: save round-trips to disk; unsaved indicators track buffer state; the conflict path never loses user bytes without an explicit choice
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 3.2 Diff view + terminal panel wired to the daemon
    - Feed CodyDiffView from the daemon diff endpoint (File Changes dropdown with per-file +/- stats, bolt's FileModifiedDropdown idiom); wire the CodyShell xterm panel to the daemon bounded exec
    - Neo's own terminal steps remain visible in the same panel history (one vocabulary — the NeoStep terminal kind)
    - Test: a real workspace change renders in the diff view with correct stats; a real exec round-trips output to the xterm panel
    - _Requirements: 5.1, 4.1_
  - [x] 3.3 Artifact/action cards in the chat rail
    - Fold each turn's editor/terminal/preview steps into one collapsible artifact card (header from narration; one row per action with live pending/running/complete/failed icon from the task 1.2 status transitions)
    - Click-through: file row opens that file in Code view; command row reveals the terminal panel; cards rebuild with final statuses from trace replay on reopen
    - Test: a real event sequence renders a card whose rows transition live and rebuild identically from the trace
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - [x] 3.4 Live-typing editor: streamed writes render progressively with force-follow
    - Render task 1.2's bounded content deltas progressively in the CodeMirror pane; on write start, auto-open the workbench, switch to Code view, and select the file (unless the user pinned a view this run)
    - Degrade honestly: a dropped delta stream falls back to the final step content, never a corrupted buffer
    - Test: a real streamed write renders progressively and converges byte-identical to the file on disk
    - _Requirements: 6.1, 6.2, 6.3, 6.4_

## P1 — Preview on Railway sandboxes

- [x] 4. A real preview without trusting the VM
  - [x] 4.1 Sandbox preview controller on the daemon + durable preview.* events
    - Build the daemon preview controller: provision an on-demand Railway sandbox (same project/environment pattern as the router provisioner's GraphQL client), sync the project workspace, start the detected dev command, health-poll to serving
    - Emit durable preview.pending/ready{url}/failed{reason}/expired events (whitelist in traceWorkspaceTypes); enforce idle expiry teardown and a per-user concurrent-sandbox cap
    - Test: lifecycle proven against a real HTTP serving target (pending→ready→expired); events replay across reopen; cap and expiry bounds hold
    - _Requirements: 7.1, 7.2, 7.4, 7.5_
  - [x] 4.2 Preview pane: iframe with address bar, reload, open-in-new-tab, honest states
    - Render the preview.ready URL in an iframe with bolt's chrome (editable address-bar path, reload, open-in-new-tab); pending/failed/expired render as honest empty/error states, never a stale frame presented as live
    - Slider Preview position reflects availability (lights up on ready); reuse the authed URL pattern where the target requires it
    - Test (RTL): the pane tracks a real preview event sequence through all four states
    - _Requirements: 7.2, 7.3_

## P2 — Cleanup and the end-to-end proof

- [x] 5. Delete the dead layer, prove the instrument
  - [x] 5.1 Delete the codyd client layer and stale panes; green build sweep
    - Delete useCody.ts, the codyd run/intent routes in lib/api/cody.ts, tiering.ts, layout-prototype/engineer/architect.tsx, cody-run-parts.tsx, cody-activity.tsx, cody-code-panes.tsx, cody-architect-panes.tsx, lib/cody/recent-runs.ts — zero imports remain
    - Sweep for retired Cody branding in rendered strings and i18n keys (all 5 locales)
    - Check: tsc + eslint + vitest green after removal
    - _Requirements: 8.3, 8.4, 1.2_
  - [x] 5.2 No-fakes end-to-end proof of the Neo workbench
    - **Property NEO-WORKBENCH: a REAL Neo run writing files and running commands in a real project workspace produces artifact cards whose rows transition through real statuses; the editor receives live-typed content converging byte-identical with disk; the diff endpoint reflects the changes; preview lifecycle is proven against a real serving target including failure/expiry; and reopen rebuilds cards + workbench state from the durable trace — real tool dispatch, real workspace files, real event stream, real client reducer, no fakes**
    - **Validates: Requirements 9.1, 9.2, 9.3**

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2", "1.3", "2.1"] },
    { "id": 2,  "tasks": ["2.2", "3.1", "3.2", "3.3", "4.1"] },
    { "id": 3,  "tasks": ["3.4", "4.2"] },
    { "id": 4,  "tasks": ["5.1"] },
    { "id": 5,  "tasks": ["5.2"] }
  ]
}
```
