<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: Cody — the next-generation coding agent

## Tasks

## P8 — Environment migration: Railway (final phase)

- [ ] 1. Provisioner abstraction, Railway provider, dev-environment image, wake-on-request
  - [x] 1.1 Extract the Provisioner interface; Fly conforms unchanged
    - Define the provider-neutral Provisioner interface in the router (ensure/create, status, wake, destroy; environment + volume) and re-point admin.StartProvision, proxy.EnsureStarted/wake.go, and the chronos wake path at the interface
    - Make router/internal/fly the first conforming implementation with behavior unchanged and its existing tests green
    - _Requirements: 1.1, 1.2, 1.4_
  - [x] 1.2 Railway provider implementation
    - Implement router/internal/railway against Railway's public API: per-user service + volume + deployment lifecycle conforming to Provisioner; config-selected provider (fly | railway) with Railway credentials in router config
    - Plumb provision status (provision_jobs, GET /provision/status) from Railway deployment readiness so onboarding gating works unchanged
    - _Requirements: 1.3, 1.4, 1.5_
  - [x] 1.3 Ubuntu 24.04 dev-environment image
    - Author the Railway environment image: Ubuntu 24.04 carrying the full daemon stack (MCL daemon, Neo, MCP bridges, later codyd) plus baked toolchains (git, build-essential, Node+pnpm, Go, Python+uv, Rust); volume mounts /data + /workspace with /workspace remaining the fs/git/exec MCP root
    - Volume seeding: BootPull from MinIO stays as the Fly->Railway migration vehicle (new volume seeds from the user's latest.tar.zst on first boot); the persistent volume is the durable state thereafter
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [x] 1.4 Wake-on-request semantics + outbound-quiet idle
    - Invert the proxy wake flow for Railway: forward-as-wake with the existing daemon-readiness probing absorbing cold start (and the possible first-request 502) within the wake budget; retry semantics the client already handles while waking; chronos /internal/wake delivers timer turns on Railway semantics; Railway Provisioner wake is a no-op (Serverless wakes on private-network traffic)
    - Outbound-quiet idle so Serverless sleep engages: disable the periodic snapshot push ticker on Railway (snapshot PushInterval < 0, already supported); audit the daemon stack for other idle outbound emitters (bridges lazy-dial, chronos wakes are inbound, Neo is event-driven) and eliminate any found
    - _Requirements: 3.1, 3.2, 3.3, 3.4_
  - [-] 1.5 Control-plane + shared-services migration into the one Railway environment
    - Move the prod-box control plane into the same Railway project/environment as the per-user services: router (public custom domain + private :8088 internal listener), gateway/chronos private-only, Postgres (managed) and MinIO (service + volume) private-only; retire the nginx front door (Railway edge terminates TLS)
    - Move the shared tool services (browser, tachyon, uwac, deus) into the same environment beside searxng/gotenberg/stalwart; flip daemon-visible URLs from Fly .internal to .railway.internal via the existing MachineEnv env overrides (config, not code); harden auth as the sole private-network boundary (constant-time bearers, strong Postgres auth, per-user scoped MinIO keys)
    - _Requirements: 16.1, 16.2, 16.3, 16.4_

## P1 — Engine contracts

- [ ] 2. Task sheet + turn-in report + workspace primitives
  - [x] 2.1 cody/ module scaffold + sheet/report contracts
    - Create the cody/ module (sibling of neo/) importing neo/internal/llm, cortex, and the delegate failure taxonomy; define the durable TaskSheet (goal, acceptance, grounding w/ files+line refs, constraints incl rules refs, verify commands, deliverable + do-not-touch) and TurnInReport (status done|partial|blocked, changes, verification evidence, gaps/assumptions) types
    - _Requirements: 5.1, 5.2, 5.3, 6.3_
  - [x] 2.2 Workspace model (persistent repo index)
    - Implement the workspace model over /workspace: tree, language/framework/build-target/entry-point detection, persisted on the volume across sessions
    - _Requirements: 10.1_
  - [x] 2.3 Edit engine with staleness detection
    - Implement anchored find/replace edits that fail if the target file drifted since read; never blind-overwrite
    - _Requirements: 10.2_
  - [x] 2.4 Verification runner (project-native commands)
    - Implement detection of the project's own verification commands (package.json scripts, Makefile, go.mod, cargo, pytest, ...) and a runner executing build/test/lint/typecheck with structured results
    - _Requirements: 10.3_
  - [x] 2.5 Checkpoint/rollback + cortex progress checkpoints
    - Implement workspace snapshot before risky multi-file changes with restore-on-request, and per-accepted-task durable progress checkpoints to cortex (continuous-memory surfaces)
    - _Requirements: 10.4, 12.1_

## P2 — Worker

- [ ] 3. Fresh-context worker runtime
  - [x] 3.1 Worker runtime: sheet in, structured report out
    - Implement the worker: fresh context seeded ONLY with the task sheet + minimal grounding bundle; full tool surface (fs/exec incl supervised services/git/browser/fetch/web-search) + edit engine + verification runner; returns the structured TurnInReport with real verification evidence and honest gaps
    - Enforce worker-side constitution structurally: read-full overflow discipline on truncated output; repo-style adherence and no out-of-scope changes per the sheet's do-not-touch
    - _Requirements: 6.1, 6.2, 6.3, 8.3, 8.7_

## P3 — Orchestrator

- [ ] 4. Plan / Spec / Delegate / Verify / Accept loop
  - [x] 4.1 Orchestrator loop (lean context, one worker at a time)
    - Implement the orchestrator cycle: understand (workspace model + cortex recall) -> plan (waved task list) -> spec next eligible task into a TaskSheet -> delegate to exactly ONE worker -> await turn-in -> verify -> accept/re-dispatch -> loop; the orchestrator never edits files nor runs implementation commands
    - On acceptance discard the worker transcript, retaining only the TurnInReport + cortex checkpoint; the orchestrator window carries plan + sheets + reports only
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - [x] 4.2 Acceptance gate: independent verify + adjudication + bounded re-dispatch
    - Implement the acceptance gate: orchestrator independently re-runs the sheet's verification commands (acceptance impossible without a green record); goal-vs-outcome adjudication of the turn-in against the sheet's acceptance criteria; rejection of no-fakes violations and weakened/deleted tests
    - Bounded re-dispatch with concrete feedback (default ceiling 3); classify worker failures via the delegate taxonomy — deterministic failures stop-and-ask instead of burning attempts; fresh worker from the same durable sheet on crash
    - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5, 6.4, 8.1, 8.2, 8.7_
  - [x] 4.3 Durable resume from cortex checkpoints
    - On codyd restart or session refresh mid-plan, reconstruct the orchestrator's lean context (plan + sheets + reports) from cortex and resume at the correct next task
    - _Requirements: 12.2_

## P4 — Modes + standards

- [ ] 5. Policy tuples, constitution surface, rules + skills injection
  - [x] 5.1 Mode policy tuples (Prototype / Engineer / Architect)
    - Implement modes as policy tuples over planning depth, verification cadence, creativity temperature, autonomy, and explanation register — one engine; Prototype = terse inline plan + milestone verify + outcome language; Engineer (default) = waved list + per-task verify; Architect = durable in-workspace spec files (EARS reqs, design, waved tasks) + property tests + cross-session resume
    - Per-role mode-aware model policy (orchestrator stronger, Prototype workers faster) through the cody gateway slot; constitution binds identically in every mode
    - _Requirements: 9.1, 9.2, 9.3, 9.4, 9.5, 9.6, 8.4, 8.5, 8.6_
  - [x] 5.2 Rules + skills injection
    - Inject rules/common/* + detected-stack rules/<language>/* into the working policy and reference the applicable rule files as sheet constraints; surface the skills/ library via the existing skill mechanism
    - _Requirements: 11.1, 11.2_

## P5 — Serving

- [ ] 6. codyd + manifest + gateway slot
  - [x] 6.1 codyd server (engine + supervisor + SSE broker + durable trace)
    - Stand up codyd in the per-user environment beside Neo following the proven serving shape (engine + session supervisor + SSE broker + durable trace), mounted behind the router under the user's JWT; no signing key, no MCL routing, no Neo/Liaison/wallet modification
    - _Requirements: 13.1, 13.4_
  - [x] 6.2 agents/cody.json manifest + 'cody' gateway slot
    - Author agents/cody.json reusing the deployed MCP bridges (fs/exec/git/browser/fetch/web-search) with no value-moving surface; register the 'cody' gateway slot (types slot + routing validSlot + rates whitelist entry; rate card values operator-owned)
    - _Requirements: 13.2, 13.3_

## P6 — Client

- [ ] 7. Coding workspace in apps/client
  - [-] 7.1 Cody coding workspace surface
    - Build the apps/client Cody workspace: chat with mode selector, workspace file tree, diff review pane, live terminal attached to the environment, dev-server preview; durable-trace + live-SSE reducer pattern so it rebuilds on reopen
    - Consumer copy shows the RESULT not the protocol (no orchestrator/worker/sheet jargon); explanation register follows the active mode; UI house rules (background-contrast separation only, no emojis/gradients/glow)
    - _Requirements: 14.1, 14.2, 14.3, 14.4_

## P7 — Verification (no fakes)

- [ ] 8. Prove provisioning, contracts, gate, economy, durability, modes
  - [x] 8.1 Provisioner conformance + wake-on-request (real fixtures)
    - Railway provider conformance against recorded API fixtures via httptest (mirroring fly_test.go) with Fly's existing tests green; proxy integration test proving the first forwarded request to a slept-service simulation succeeds within the wake budget
    - **Property 1: provisioning is provider-neutral and wake is request-driven**
    - **Validates: Requirements 1.2, 1.3, 3.1, 15.1, 15.2**
  - [x] 8.2 Sheet/turn-in round-trip on a seeded real repo
    - End-to-end real test: orchestrator authors a sheet for a seeded real repo task; a real worker executes it in a real workspace (tempdir git repo); the report carries real verification output
    - **Property 2: the delegation contract executes real work with real evidence**
    - **Validates: Requirements 5.1, 5.2, 6.1, 6.3, 15.3**
  - [x] 8.3 Acceptance gate is structural + re-dispatch policy
    - Real tests: a turn-in claiming success with red verification is REJECTED; a green turn-in is accepted and checkpointed to real cortex; a no-fakes violation (stub/fake introduced to pass) is rejected; weakened/deleted tests cause rejection; deterministic failure stops-and-asks; transient failure re-dispatches bounded by the ceiling
    - **Property 3: acceptance cannot be talked into — only proven**
    - **Validates: Requirements 7.1, 7.2, 7.3, 7.4, 8.1, 8.2, 8.7, 15.4, 15.5**
  - [x] 8.4 Context economy + durable resume
    - Real tests: after N accepted tasks the orchestrator's real message window holds plan + sheets + reports only (no worker transcripts); killing codyd mid-plan and restarting resumes from cortex checkpoints at the correct next task
    - **Property 4: the orchestrator stays lean and survives anything**
    - **Validates: Requirements 4.4, 4.5, 12.2, 15.6, 15.7**
  - [-] 8.5 Mode differentiation under one constitution
    - Real tests: the same seeded task under Prototype vs Architect exhibits the policy differences (plan depth, verify cadence, explanation register) while both hold the constitution (verify-before-done gated in both)
    - **Property 5: modes dial ceremony, never the standard**
    - **Validates: Requirements 9.2, 9.4, 9.5, 15.8**
  - [ ] 8.6 Client workspace reducer tests
    - vitest reducer tests for the coding-workspace event families (rebuild-on-reopen from durable trace + live SSE application), mirroring the NeoTask pattern
    - **Property 6: the workspace surface is durable and live**
    - **Validates: Requirements 14.2, 15.9**

- [ ] 9. Final checkpoint — Cody complete
  - All six properties green. The per-user environment runs on Railway (Ubuntu 24.04 service + volume, /data + /workspace, wake-on-request scale-to-zero) behind a provider-neutral Provisioner with Fly intact as fallback. codyd serves the Cody engine beside Neo: an orchestrator that Plans, Specs, and Delegates — one fresh-context worker per task executing a self-contained sheet, turn-ins independently verified (green verification + goal-vs-outcome adjudication) before acceptance, bounded classified re-dispatch, worker transcripts discarded so the orchestrator stays lean over long horizons, progress durable in cortex and resumable across restarts. Modes Prototype / Engineer / Architect dial ceremony while the engine-enforced constitution (no fakes, verify-before-done, read-full, honest partials, complete artifacts, user drives git, respect the project) never bends. Standards from rules/, playbooks from skills/, tools from the deployed MCP bridges, metered on the 'cody' slot, no MCL/signing/wallet surface. The client ships a coding workspace (chat + mode dial, file tree, diff review, terminal, preview) that shows the result, not the protocol. Deploy is user-driven.

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["2.1"] },
    { "id": 2,  "tasks": ["2.2", "2.3", "2.4"] },
    { "id": 3,  "tasks": ["2.5", "3.1"] },
    { "id": 4,  "tasks": ["4.1", "5.2"] },
    { "id": 5,  "tasks": ["4.2", "4.3", "5.1", "8.2"] },
    { "id": 6,  "tasks": ["6.1", "6.2", "8.3", "8.4"] },
    { "id": 7,  "tasks": ["7.1", "8.5"] },
    { "id": 8,  "tasks": ["8.6"] },
    { "id": 9,  "tasks": ["1.1"] },
    { "id": 10, "tasks": ["1.2", "1.3"] },
    { "id": 11, "tasks": ["1.5"] },
    { "id": 12, "tasks": ["1.4"] },
    { "id": 13, "tasks": ["8.1"] }
  ]
}
```
