<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: Cody Launch — commercial-grade hardening (the Claude Code bar)

## Tasks

## P0 — Regression-safety unblock (the test suites must compile before anything is trusted)

- [ ] 1. Restore the safety-critical test suites
  - [ ] 1.1 Neo agent test package compiles + passes (real Verdicts, no fakes)
    - Update neo/internal/agent tests to the current API: 3-arg verdictAccepts -> 2-arg, 6-arg buildAuditContract -> 5-arg, remove/replace the salientTokens test — against REAL cassandra.Verdict values asserting the pure mapping, never a fake
    - Make neo go build ./... , go vet ./... , go test ./... green
    - _Requirements: 1.1, 1.3_
  - [ ] 1.2 Cassandra test package compiles + passes (transcript-judged behavior)
    - Re-express cassandra tests that referenced the removed CheckCitations against the current transcript-judged verdict behavior (build a real Verdict / run the real mapping)
    - Make cassandra go build ./... , go vet ./... , go test ./... green; add a check that forbids a non-compiling or red module test package
    - _Requirements: 1.2, 1.3, 1.4_

## P1 — Cody runtime blockers (the audit's Y-series)

- [ ] 2. Path seam, gate evidence, capability-aware UI verification, unsatisfiable-gate detection
  - [ ] 2.1 Unified workspace-path normalization seam at the tool boundary (Y2)
    - Add ONE normalization function (resolve against root, reject escapes, return clean workspace-relative) and route worker.toolList/toolRead/toolWrite/toolDelete through it — eliminating the filepath.Join double-join on absolute in-root paths
    - Make worker.recordChange store the normalized workspace-relative path so report.Changes never carries an absolute path; table-driven test proving abs-in-root == relative behavior
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [ ] 2.2 Gate adjudication sees the real changed source (Y1)
    - Make gate.renderSource relativize a report.Changes path against the root before reading (absolute in-root path included, never skipped); make the do-not-touch + test-weakening screens compare normalized paths
    - Test: a worker writes an absolute-path file; the gate's evidence contains the file source; the real adjudicator grounds on it
    - _Requirements: 3.1, 3.2, 3.4_
  - [ ] 2.3 Planner + sheets emit workspace-relative paths (Y1 upstream)
    - Make the planner emit workspace-relative paths in a sheet's goal, verification commands, and grounding so the worker is primed with the relative shape (verification still runs from the workspace root)
    - _Requirements: 3.3_
  - [ ] 2.4 Screenshot capability probe + typed contract (Y3, Y4)
    - Probe screenshot capability (browser service reachable) and expose it as a typed signal; make browserScreenshot return 'not configured' as a typed result, never a message in the path slot; ensure no caller records that message as a screenshot path
    - _Requirements: 4.1, 4.4_
  - [ ] 2.5 Graceful UI-verification degradation + unsatisfiable-gate detection (Y3, Y6)
    - When screenshot capability is present the gate binds; when absent it degrades to a non-blocking advisory + a deterministic render check (never unsatisfiable); orchestrator detects a repeated no-action-possible gate rejection and stops-and-asks or replans instead of burning attempts
    - Tests: absent-capability UI turn-in accepted via degradation; present-capability screenshot gate binds; unsatisfiable gate stops-and-asks rather than consuming all MaxAttempts
    - _Requirements: 4.2, 4.3, 5.1, 5.2, 5.3_
  - [ ] 2.6 Absolute-path + gate E2E regression (Y9 — the 'make me a video' shape)
    - End-to-end regression over the logged failure shape: a real orchestrator+worker run on a seeded UI task with absolute paths is now gate-accepted (evidence grounded, gate satisfiable), proving Y1+Y2 fixed together
    - **Property A: the absolute-path adjudication-blindness class is gone**
    - **Validates: Requirements 2.2, 2.4, 3.1, 3.4, 13.2**

## P2 — Shared faculties: Neo completion loop + Cassandra

- [ ] 3. One acceptance predicate, fail-toward-refusal, bounded Neo loop
  - [ ] 3.1 One shared acceptance predicate (X1)
    - Make cassandra.Sound() the single source of truth for verdict acceptance; repoint neo.verdictAccepts and Cody's gate.Adjudicate to call it (or a shared exported predicate) instead of re-implementing the rule inline; test the same Verdict decides identically in all three
    - _Requirements: 6.1, 6.2, 6.3_
  - [ ] 3.2 Cassandra fails toward refusal + escalation keeps the stricter verdict (C2, C3)
    - Normalize a parsed verdict missing coverage/groundedness signals TOWARD refusal (not coverage=full/grounded=true); make tiered escalation keep the stricter/more-certain verdict rather than unconditionally replacing the primary
    - Tests over real degenerate/low-confidence Verdicts proving refusal-by-default and stricter-wins escalation
    - _Requirements: 7.1, 7.2_
  - [ ] 3.3 Goal-type classification coverage with a grounding guard (C5)
    - Pin the question/status/truthful-no classification with tests including a grounding guard, so an UNCHECKED negative cannot pass as complete
    - _Requirements: 7.3_
  - [ ] 3.4 Neo completion loop bounded by unified unproductive-attempt accounting (N2, N3, N4)
    - Introduce one unproductive-attempt counter fed by completion-gate rejections, no-progress stalls, and guidance nudges; only genuine accepted progress resets it (a genuine tool call between completion rejections does NOT silently reset the completion-reject bound); the completion branch advances the no-progress read before it continues
    - _Requirements: 8.1, 8.2, 8.3_
  - [ ] 3.5 Faculty regression: bounded Neo loop + degenerate-verdict safety (N2, C2/C3)
    - Real tests: an interleaved work->premature-complete->reject loop terminates with an honest partial within the unified bound, well before the step budget; the same degenerate/low-confidence Verdict is rejected identically by Neo, Cody, and Cassandra
    - **Property B: the acceptance interaction is bounded and fails safe across modules**
    - **Validates: Requirements 6.3, 7.1, 7.2, 8.4, 13.2**

## P3 — Cody depth & economy

- [ ] 4. Recon economy, ceremony fast-path, the real spec pipeline
  - [ ] 4.1 Fresh-worker recon economy — cached workspace map in the sheet (Y7)
    - Carry the orchestrator's workspace model (tree/languages/entry points) as a cached map in the sheet grounding; make the worker use it for orientation and skip the redundant full-workspace scan; test proves no redundant scan on a seeded repo
    - _Requirements: 9.1, 9.2, 9.3_
  - [ ] 4.2 Ceremony fast-path for low-stakes / Prototype intents (Y5)
    - Collapse the SDR + multi-candidate DLR + multi-candidate planner ceremony into a lean start for low-stakes/Prototype intent, constitution binding identically; test proves materially fewer pre-code LLM calls than full ceremony while both hold the constitution
    - _Requirements: 10.1, 10.2, 10.3_
  - [ ] 4.3 The intended spec pipeline — spec.kvx + design.body.md via specgen (Y8)
    - Make Architect mode author a real spec.kvx + design.body.md and invoke specgen to render requirements.md/design.md/tasks.md, replacing the hand-rolled .cody/spec markdown; test the authored kvx round-trips through the real specgen parser
    - _Requirements: 11.1, 11.4_
  - [ ] 4.4 Research + clarifying-questions phases precede planning (Y8)
    - Add a research phase (grounding gather) before the plan is authored, and a clarifying-questions phase over the answer channel when the ask is underspecified; test proves both gate planning
    - _Requirements: 11.2, 11.3, 11.4_

## P4 — Coding UI to the Claude Code bar (harden + polish the built surface)

- [ ] 5. Design conformance, progress/narration, diff/tree/terminal/preview, plan board
  - [ ] 5.1 Design-quality audit + house-rules conformance of the built /cody surface
    - Audit the built /cody surface (spec/cody-client, spec/cody-smoothness) against rules/web/design-quality.md; fix any drift to the house rules: separation by background-color contrast only (no border strokes for depth), no emojis, no purple/indigo gradients, no glow
    - _Requirements: 12.1_
  - [ ] 5.2 Progress spine + narration hardening (never blank, including approve->next)
    - Verify + harden the always-live progress spine so the viewport is never blank while running or awaiting (including the approve->next-phase gap) and Cody narrates as it works; verified against a real run
    - _Requirements: 12.2_
  - [ ] 5.3 Diff review + file tree fidelity
    - Bring diff review and the file tree to faithful, navigable, real state (real diffs with syntax, real tree navigation) at the Claude Code bar
    - _Requirements: 12.3_
  - [ ] 5.4 Live terminal + dev-server preview pane fidelity (the user SEES the result)
    - Bring the live terminal (attached to the environment) and the dev-server preview pane (wired to the preview Manager) to faithful live state, so a 'make me a X' ask ends with the user seeing the running result
    - _Requirements: 12.3_
  - [ ] 5.5 Plan board + turn-in evidence cards in result language
    - Bring the waved plan board and turn-in cards to real verification-evidence display in RESULT language, zero orchestrator/worker/sheet jargon in consumer copy, register following the active mode
    - _Requirements: 12.4_

## P5 — Proof & launch

- [ ] 6. Creative-build E2E, full green + coverage
  - [ ] 6.1 Creative-build E2E — real preview + real screenshot, gate-accepted, no fakes
    - Seeded 'make me a small web app' run to a real running dev-server preview and a real screenshot artifact, gate-accepted, with NO stub/mock/fake anywhere in the path — the exact ask that was 'dog shit' now succeeds and leaves a viewable result
    - **Property C: a creative build produces a viewable, verified result**
    - **Validates: Requirements 4.2, 4.3, 5.2, 12.3, 13.1**
  - [ ] 6.2 Full green + coverage gate across cody + neo + cassandra + client
    - go build/vet/test ./... (with -race where concurrency is touched) green across cody, neo, cassandra; client coding-workspace reducer tests green; a coverage gate holds on the changed packages; no fakes anywhere
    - **Property D: the whole stack is green and covered**
    - **Validates: Requirements 1.3, 13.2, 13.3, 13.4**

- [ ] 7. Final checkpoint — Cody launch-ready
  - All four properties green. The absolute-path adjudication-blindness class is gone (one path seam; gate sees real source; planner/worker use relative paths). UI verification is capability-aware and always satisfiable; an unsatisfiable gate stops-and-asks rather than burning attempts. The Neo completion loop is bounded by unified unproductive-attempt accounting; one shared cassandra.Sound() predicate governs acceptance across Neo, Cody, and Cassandra, failing toward refusal on degenerate verdicts. The safety-critical test suites compile and cover the failure shapes. Cody starts faster (ceremony fast-path), starts oriented (cached recon), and plans to the repo's own bar (real spec.kvx/design.body.md via specgen, preceded by research + clarifying questions). The built /cody coding surface meets the Claude Code bar (design conformance + house rules, always-live progress + narration, faithful diff/tree/terminal/preview, plan board + real evidence cards in result language). A seeded creative build produces a real running preview and a real screenshot, gate-accepted, with no fakes. Every session was worked under the baked-in protocol at commercial/production grade; the attestation task confirms the protocol was followed and the quality bar held. Deploy is user-driven.
  - [ ] 7.1 Launch-readiness attestation
    - Confirm all four properties green and the whole stack build/vet/test green; write the final launch-readiness handoff + cortex outcome; attest the protocol was followed and the quality bar held
    - _Requirements: 13.1, 13.2, 13.3, 13.4, 14.3_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["2.1", "3.1", "5.1"] },
    { "id": 3,  "tasks": ["2.2", "2.3", "3.2", "3.4", "4.1"] },
    { "id": 4,  "tasks": ["2.4", "3.3", "4.2", "4.3", "5.2"] },
    { "id": 5,  "tasks": ["2.5", "2.6", "3.5", "4.4", "5.3"] },
    { "id": 6,  "tasks": ["5.4", "5.5"] },
    { "id": 7,  "tasks": ["6.1", "6.2"] },
    { "id": 8,  "tasks": ["7.1"] }
  ]
}
```
