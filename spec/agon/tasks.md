<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: AGON — the Matrix Agent Qualification standard

## Tasks

## P0 — Foundations: taxonomy, scenario schema, golden-fixture validation

- [ ] 1. The versioned standard core
  - [x] 1.1 Capability taxonomy v0 partitioned by the three suites
    - Author the versioned taxonomy artifact under dojo/ (schema-validated): three suites (Raw Model, Agentic, MRA), their realms, and leaf capabilities each with a definition, a falsifiable pass condition, difficulty tiers, and a minimum item count
    - Add a validator + test proving every leaf is well-formed and the suite/realm/leaf tree is internally consistent
    - _Requirements: 1.1, 1.2, 1.3_
  - [x] 1.2 Declarative scenario schema + loader + validation
    - Define the declarative scenario schema (id, suite, capabilities, difficulty, kind, seed_source, files, sheet, verify, ground_truth, scorer, sut_config, contamination_class) and a loader that rejects unknown/missing fields and unknown capabilities
    - Test: a malformed scenario is rejected; a scenario referencing a non-taxonomy capability is rejected
    - _Requirements: 2.1, 2.2, 2.3, 1.4_
  - [x] 1.3 Golden-fixture scorer + verify self-validation harness
    - Add a fast offline validation pass: each scenario declares golden-correct and golden-incorrect fixtures; assert scorer scores correct high / incorrect low and every verify command exits 0 on correct and non-zero on incorrect; generalize the score_planner golden-dir check
    - Wire it as a stop-condition check; test proves a deliberately-broken scorer fails validation
    - _Requirements: 3.1, 3.2, 3.3_
  - [x] 1.4 Port the existing 10 scenarios onto the schema (behavior-preserving)
    - Port w_ratelimiter, w_static_html, w_broken_gate, w_impossible_ac, w_silent_tool, w_tight_budget, n_correction, p_author_sheet, c_sdr_json, g_judge_fake onto the declarative schema with golden fixtures, tagged to taxonomy leaves
    - Test proves the port is behavior-preserving: identical observable scores/flags versus the pre-port harness on the same seeded inputs
    - _Requirements: 2.4_

## P1 — Scale, isolation, provenance

- [ ] 2. Make it fast, safe, and reproducible
  - [ ] 2.1 Bounded-concurrency work queue + provider registry
    - Replace the serial model/scenario/rep loop with a bounded-concurrency work queue; generalize MODEL_ROUTES into a pluggable provider registry (endpoint, key env, served id, budget field, extra body, per-provider concurrency, retry/backoff, cost accounting, and a tools-unsupported fallback per provider)
    - Test: concurrent execution honors per-provider limits and produces the same scores as serial on a seeded fixture provider
    - _Requirements: 8.1, 8.2_
  - [ ] 2.2 Resumable, re-scorable run cache
    - Content-address raw transcripts so a run is resumable and can be re-scored without re-invoking the model
    - Test proves re-scoring a cached run reproduces identical scores
    - _Requirements: 8.3_
  - [ ] 2.3 Real sandbox isolation for exec + verify
    - Run workspace exec and verify_run in an isolated sandbox (container or rootless namespace), constrained fs, no network by default, preserving VERIFY_PATH; degrade to a clearly-labeled reduced-isolation mode when unavailable
    - Test proves a poisoned tool shim planted in the workspace cannot reach the verify path under isolation
    - _Requirements: 9.1, 9.2, 9.3_
  - [ ] 2.4 Signed provenance result cards + reproducibility
    - Emit a result card stamping model/served id, harness+standard version, corpus content hash, per-item seeds, temperature policy, raw transcripts, scores, and CIs; integrity-stamp it (hash/signature)
    - Tests: a modified card fails verification; a deterministic-eligible item reproduces exactly given the same corpus hash + version + seeds
    - _Requirements: 10.1, 10.2, 10.3_

## P2 — Scoring methodology and the AQI

- [ ] 3. Calibrated, confidence-bounded, citable
  - [ ] 3.1 Confidence intervals + flake accounting
    - Compute mean + bootstrap CI per item over N reps; report flake rate; exclude API-error runs transparently; every reported score carries a CI
    - _Requirements: 11.1_
  - [ ] 3.2 Item-discrimination calibrated weights
    - Derive realm/slot weights from item discrimination against a reference panel of strong/weak models, replacing the hand-tuned constants in run.py aggregate(); persist the calibration
    - Tests prove calibration is reproducible from the reference-panel data, that removing a high-discrimination item changes the weights, and that a dead (non-discriminating) item is flagged
    - _Requirements: 11.2, 11.4_
  - [ ] 3.3 Hard DQ gates + adversarial anti-Goodhart items
    - Keep hard DQ flags absolute (cap the index at zero regardless of aggregate); add adversarial anti-Goodhart items to the corpus that punish metric-gaming
    - _Requirements: 11.3_
  - [ ] 3.4 The AQI: MPI/APS/RAS sub-indices + headline + economics
    - Compose the AQI from MPI (Suite 1), APS (Suite 2), RAS (Suite 3), each with CIs + per-realm profile; put the documented composition formula in the standard artifact; make economics a first-class reported axis
    - _Requirements: 12.1, 12.2, 12.3_

## P3 — Suite content: Raw Model, Agentic, corpus breadth

- [ ] 4. Fill the suites
  - [ ] 4.1 Suite 1 Raw Model realms + items + MPI
    - Build Suite 1 items across its realms (reasoning incl algebra/logic-puzzle/constraint-satisfaction/inductive, knowledge incl the anti-hallucination probe, instruction-following, structured output incl YAML/CSV + grammar/escaping edges, long-context incl multi-needle/distractor/cross-reference/read-full, calibration/honesty, refusal/safety, stability, token economy, multilingual/cross-tokenizer, adversarial-prompt robustness) with deterministic oracles where they exist; reach each covered leaf's min_items; emit the MPI with per-realm CIs
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [ ] 4.2 Suite 2 Agentic realms + slots + APS
    - Consolidate the ported agentic scenarios into Suite 2 realms (competence, tool mastery/recovery incl correct-tool-selection, discipline incl no-redundant-rescan, integrity-under-agency, planning, adjudication incl accept-genuine + reject-trivial, adaptivity, scope/safety incl destructive-restraint + workspace-escape, multi-file/multi-step project, in-task state/memory, anti-Goodhart traps); post-hoc ground truth; hard DQ caps slots; emit APS with worker/planner/judge/neo slot scores + CIs
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [ ] 4.3 Corpus breadth: synthetic-parametric + adversarial families + contamination classes
    - Add synthetic-parametric item templates (fresh, non-repeating instance per run), adversarial trap items, and canary/holdout leak-detection items; assign contamination_class (public/holdout/rotating); reach each covered leaf's minimum item count
    - Test proves no holdout/rotating item content appears in any rendered/published output, and that a parametric item does not repeat its instance across runs
    - _Requirements: 2.3, 2.5, 15.3_

## P4 — Suite 3: the Matrix Receptive Agent and the Cassandra proof

- [ ] 5. Ablation scoring + the silent-voice controller
  - [ ] 5.1 MRA ablation runner + sut_config variants
    - Build the Suite 3 runner that executes each scenario in sut_config variants (main-only, main+cassandra, main+cassandra+cortex) and scores the DELTA between variants as the primary product; realms loop-kill/self-heal, doubt-vs-assurance, guardrail enforcement, memory continuity, multi-session continuity, emergent integrity, composition economics, and negative/no-regression ablation, each a delta with CI
    - _Requirements: 6.1, 6.2, 6.3, 6.4_
  - [ ] 5.2 Cassandra controller hook + dual-record audit
    - Expose the between-turns controller hook that may edit the previous assistant content in place (content only; gated by loop/stall; turn>=2; one mod/turn); dual-record original_content + cassandra_mod + trigger as run artifacts
    - Tests prove no modification ever alters facts or touches non-content fields
    - _Requirements: 7.1, 7.2_
  - [ ] 5.3 Canonical items + the loop-kill / stay-silent proof
    - Encode the two canonical items (deepseek-v4-pro w_broken_gate 22-step spiral loop-kill target; mimo-2.5-pro clean 9-step stay-silent baseline) as Suite 3 MRA scenarios
    - **Property Cassandra: enabling the controller kills the loop on the target (bounded before budget, honest partial) and stays silent on the healthy baseline (no mod fired), shown by the ablation delta**
    - **Validates: Requirements 7.3, 7.4, 6.1**

## P5 — Governance, CI gate, and the published standard

- [ ] 6. Make it a standard
  - [ ] 6.1 CI qualification/promotion gate
    - Add a gate mode comparing a candidate's per-slot/per-suite indices to a stored baseline; FAIL on regression beyond threshold or any hard DQ; emit a machine-readable verdict + the report; single-command CI-runnable
    - Tests: a seeded regressing candidate fails; a seeded non-regressing candidate passes
    - _Requirements: 14.1, 14.2, 14.3_
  - [ ] 6.2 Reporting to the house standard
    - Render slot leaderboard, per-realm profiles with CIs, AQI + sub-indices, Suite 3 ablation deltas, economics table, flags; link every score to its evidence; house rules (no border-as-depth, no emojis, no purple/indigo gradients, no glow)
    - _Requirements: 13.1, 13.2, 13.3_
  - [ ] 6.3 Published methodology doc + versioning + governance
    - Generate/pin a human-readable methodology document from the versioned source artifacts (taxonomy, schema, scoring, AQI composition, contamination policy); stamp the standard version on every card; require a version bump + migration note on a breaking change
    - _Requirements: 15.1, 15.2_

## P6 — Proof and launch

- [ ] 7. No-fakes end-to-end
  - [ ] 7.1 End-to-end no-fakes proof on real models
    - Run all three suites on the reference-panel models to a complete signed result card + report; run the Suite 3 Cassandra proof through the ablation (loop-kill on target, silence on baseline, dual-record present); no stub/mock/fake anywhere in the scoring path
    - **Property AGON: the whole instrument produces a reproducible, provenance-stamped, confidence-bounded result on real models with no fakes**
    - **Validates: Requirements 16.1, 16.2, 16.3**
  - [ ] 7.2 Launch-readiness attestation
    - Confirm golden-fixture validation, re-score reproducibility, provenance-tamper, and holdout-leak tests all green; write the final launch-readiness handoff + cortex outcome; attest the protocol was followed and the quality bar held
    - _Requirements: 16.3, 17.3_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["1.3", "1.4"] },
    { "id": 3,  "tasks": ["2.1"] },
    { "id": 4,  "tasks": ["2.2", "2.3", "2.4"] },
    { "id": 5,  "tasks": ["3.1", "3.2", "3.3"] },
    { "id": 6,  "tasks": ["3.4", "4.1", "4.2"] },
    { "id": 7,  "tasks": ["4.3", "5.1"] },
    { "id": 8,  "tasks": ["5.2", "5.3"] },
    { "id": 9,  "tasks": ["6.1", "6.2", "6.3"] },
    { "id": 10, "tasks": ["7.1"] },
    { "id": 11, "tasks": ["7.2"] }
  ]
}
```
