<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: Neo × KindleLaunch Autonomy

## Tasks

## P1 — kindle MCP bridge (reads + write encodings)

- [x] 1. kindle bridge scaffold + canonical config
  - [x] 1.1 Create tools/kindle/lib/config.mjs with the canonical chain-125 address book + env overrides
    - Encode Router/FeesRouter/FeeAccumulator/Quoter/PoolRegistry/SidioraNFT/SidioraFactory/OpticalRegistry + preset opticals + USDL/SID from the 2026-06-20 manifest (protocol/addresses.go / paxeer-addresses.json); every value env-overridable; USDL decimals=6
    - Explicitly NOT reuse paxeer-net config sidioraFun addresses (different/older deployment); document why in-file
    - _Requirements: 3.2, 3.3, 11.2_
  - [x] 1.2 Create the kindle stdio bridge (kindle.mjs) mirroring paxeer-net wire protocol + dispatch + --selftest
    - initialize / tools/list / tools/call over newline-delimited JSON-RPC; structured tool errors; READS_ONLY split that withholds write tools
    - --selftest lists the registry and verifies bijection against every agents/*.json declaring a kindle server, exiting non-zero on drift
    - _Requirements: 3.1, 3.4, 3.6, 1.4_
  - [x] 1.3 Implement read tools (pools, token/market detail, quote, pending fees, optical lookup)
    - kindle_pools (PoolRegistry/indexer discovery), kindle_market (BFF/token detail), kindle_quote (Quoter), kindle_fees (FeeAccumulator.getAccumulatedFees + airdrop/LP balances), kindle_optical_info (OpticalRegistry.getOpticalMetadata/isRegistered); decode amounts to human units with correct decimals
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 7.4_
  - [x] 1.4 Reuse the embedded-wallet write lane (lib/wallet.mjs + agentauth.mjs) for kindle writes
    - Share the paxeer embedded-wallet client (agent-native DID auth, /v1/agent/send) so kindle write tools sign server-side; no second signing path; spend within wallet policy (403 surfaced)
    - _Requirements: 1.3, 2.3, 2.4, 16.3_

## P2 — Launch / Trade / Collect-fees write tools

- [x] 2. Encode the value-moving KindleLaunch tools
  - [x] 2.1 Implement kindle_launch (createMarket / createMarketWithPermit + USDL creation-fee approve)
    - Map feeStrategy name→enum (0..3, reject unknown); ensure USDL approve to Router or use permit; pass optical (zero addr when none); return token/pool/nftId + explorer link; surface revert/denial in plain language
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6_
  - [x] 2.2 Implement kindle_buy / kindle_sell / kindle_swap with quote-bounded slippage + deadline
    - buy/sell/swapTokenForToken (+permit variants); derive minOut from a Quoter quote × slippage tolerance via integer math; finite deadline; approve token/USDL as needed; base-unit amounts with correct decimals
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 11.1, 11.3_
  - [x] 2.3 Implement kindle_collect_fees / kindle_set_fee_strategy (NFT-owner gated, strategy-matched)
    - Verify embedded-wallet owns SidioraNFT(nftId) before acting; route to claimFees/executeBurn/executeAirdrop/executeLpRewards by current strategy; setFeeStrategy when changing; decline (not revert) on not-owner / wrong-strategy / no-fees
    - _Requirements: 7.1, 7.2, 7.3, 7.5_

## P3 — Create Opticals (preset + custom-via-Tachyon)

- [x] 3. Optical creation surface
  - [x] 3.1 Implement preset-optical attach (+ factory-deploy where available)
    - Expose the 5 deployed preset opticals (descriptions) for attach-at-launch; if a self-service factory (LaunchpadOpticalFactory) is on the manifest, deploy a configured instance via the embedded wallet; otherwise say so and offer singletons or custom path
    - _Requirements: 9.1, 9.2, 9.3, 9.4, 9.5_
  - [x] 3.2 Implement custom-optical authoring template + Tachyon compile/test/deploy orchestration
    - BaseOptical-derived authoring template (override only needed hooks; getFlags bitmap must match overrides; preserve onlyPool/onlyOwner/immutability); drive tachyon_compile → tachyon_test → tachyon_deploy (chain 125, idempotency_key, constructor poolRegistry+owner); no deploy on failed compile/test
    - Communicate that a fresh optical is functional but unverified (OpticalRegistry registration is admin-only); never block on registration; attach via createMarket optical param
    - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5, 10.6_

## P4 — Wiring + skill + approval-model evolution

- [ ] 4. Make the capability live and planner-aware
  - [ ] 4.1 Register the kindle server in agents/default.json (bijection + package_digest) + image bake + router env
    - Add alias kindle (node tools/kindle/kindle.mjs) with the exact advertised tool set; real package_digest; COPY tools/kindle into the daemon Dockerfile; inject any kindle env via router MachineEnv (no entrypoint change beyond env)
    - _Requirements: 3.5, 16.1, 16.2, 16.4_
  - [ ] 4.2 Author the kindle-launchpad skill (SKILL.mtx + SKILL.md) + expose tools to the freeform path
    - Declare the kindle write tools + procedures (launch/trade/collect/optical) with on-block hints; double-quote all string KV; subset/exact-match the kindle manifest tools; add the kindle tools to the default assistant skill so plain-English requests reach them
    - _Requirements: 15.1, 15.2, 15.3, 15.4_
  - [ ] 4.3 Evolve the approval model in neo.frozen.kvx + core_execute/escalate copy
    - Document the pre-authorized leash model (mode + caps on the embedded wallet) as the approval model for KindleLaunch autonomy; keep the in-conversation gate for one-off non-pre-authorized money; update the core_execute schema description + [relation.delegation].approval_ux + [execution.escalate] wording accordingly; preserve backstop_1 (Neo holds no key)
    - _Requirements: 2.1, 2.2, 2.5_

## P5 — Watch loop (recurring autonomy)

- [ ] 5. Recurring monitoring + triggered action
  - [ ] 5.1 Implement the Watch procedure (chronos alarm_set + wake re-evaluation + triggered dispatch)
    - On a watch request, set a chronos alarm (once/cron) with a contextful wake_message + payload; on wake, read state via kindle read tools, evaluate the trigger, and dispatch trade/collect via core_execute when met; leave/reschedule when not met; alarm_list/alarm_cancel for management
    - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5_
  - [ ] 5.2 Honor task-durability + leash-denial handling in the watch loop
    - Watch-triggered tasks decouple from the user connection and survive restart/suspend (continue on a fresh agent); on a 403 leash denial, stop the action, keep/cancel per instruction, surface plainly (no silent looping)
    - _Requirements: 8.6, 8.7, 2.3_

## P6 — Verification

- [ ] 6. Prove it (no fakes)
  - [ ] 6.1 Bridge drift selftest + read-tool decode tests
    - node tools/kindle/kindle.mjs --selftest passes against the real agents/default.json kindle entry; unit tests for amount decode with correct per-token decimals
    - **Property 1: bridge↔manifest bijection + decimal-correct decode**
    - **Validates: Requirements 3.4, 3.5, 4.2, 11.2**
  - [ ] 6.2 Calldata + quote-bounded-minimum verification via real ABIs and read-only simulation
    - Assert createMarket/buy/sell/swap/claimFees calldata encodes against real ABIs; assert minOut bounds via integer math on a real Quoter read; tachyon_simulate / eth_call dry-runs where possible; no broadcast without explicit user approval + funded leash
    - **Property 2: encodings + slippage bounds are real and correct**
    - **Validates: Requirements 5.1, 6.1, 6.4, 7.1, 11.1, 11.3, 12.2**
  - [ ] 6.3 Custom-optical real compile/test via Tachyon
    - A BaseOptical-derived sample compiles with real solc and passes a real Forge test through tachyon_compile/tachyon_test; getFlags bitmap matches overrides; deploy gated on green
    - **Property 3: custom optical is genuinely buildable (real toolchain)**
    - **Validates: Requirements 10.1, 10.2, 12.3**
  - [ ] 6.4 D11 non-perturbation check for the read/watch side-channels
    - Assert that enabling the kindle read tools + watch loop leaves MCL's recorded replayable-input sequence byte-identical vs disabled; value-moving ops ride the normal signed MCL path
    - **Property 4: side-channel non-perturbation (D11)**
    - **Validates: Requirements 13.1, 13.2, 13.3, 13.4**
  - [ ] 6.5 End-to-end on-box rehearsal (gated on explicit user approval)
    - With Andrew's explicit YES + a funded test wallet/leash: provision a user daemon, leash trade_only, run a freeform launch → buy → set-watch → collect-fees → create-optical flow; verify outcomes + plain-language surfacing + 403 handling
    - _Requirements: 2.1, 2.2, 2.3, 5.1, 6.1, 7.1, 8.1, 9.2, 10.3, 12.4, 14.1, 14.2_

- [ ] 7. Final checkpoint — Neo KindleLaunch autonomy complete
  - All capability tests green; approval-model docs updated; deploy is user-driven (Andrew drives prod redeploys). Ask the user if questions arise.

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1"] },
    { "id": 2,  "tasks": ["1.2"] },
    { "id": 3,  "tasks": ["1.3", "1.4"] },
    { "id": 4,  "tasks": ["2.1", "2.2"] },
    { "id": 5,  "tasks": ["2.3", "3.1"] },
    { "id": 6,  "tasks": ["3.2"] },
    { "id": 7,  "tasks": ["4.1", "4.2"] },
    { "id": 8,  "tasks": ["4.3", "5.1"] },
    { "id": 9,  "tasks": ["5.2", "6.1"] },
    { "id": 10, "tasks": ["6.2", "6.3", "6.4"] },
    { "id": 11, "tasks": ["6.5"] }
  ]
}
```
