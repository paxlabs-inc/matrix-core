<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: EPISTEMIC-CORE — residency substrate, then the four mechanisms, then the corpus proofs

## Tasks

## P0 — Layer 0: the residency substrate

- [x] 1. Re-base the window, make the self-model resident, freeze the prefix
  - [x] 1.1 Window re-base + budget derivation at both scales
    - Re-base ContextWindowTokens default to the main model's true window (1000000) in neo/internal/config/config.go; keep env/kvx overrides authoritative; re-derive soft/hard budget headroom sane at the new scale with the small-window path intact
    - Audit consumers (cmTrimWorking, compaction triggers, activation budgets) so trimming fires only near true pressure and within-turn tool evidence is retained while ample window remains
    - Test: budget derivation + real trim/compaction behavior at 1M and 32K configurations
    - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - [x] 1.2 Capability-surface builder: full self-model rendered into the stable prefix
    - Build the generator that derives the capability-surface section from the self-model artifact (SelfModelGraph): external API surface, tool inventory with one-line semantics, is/is-not architectural facts, failure patterns — honest 'unknown' for gaps, never fabricated
    - Render it into the byte-stable system prefix (prompt.go seam), replacing the compressed-summary-only posture; keep recallSelf for source-level depth; ensure the artifact carries the true external surface facts
    - Test: rendered section carries the true surface (POST /chat + SSE /events; no OpenAI-compatible endpoint) from a real artifact; byte-identical across a turn's steps
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [x] 1.3 Byte-stable prefix discipline: pinned once per turn, fixed tail order
    - Compute the pinned block once per turn (NE-7 fix) with mid-turn cortex writes deferred to the next turn's snapshot; establish the fixed tail section order (activation bundle, premise ledger, task graph)
    - Test: prefix byte-stability across a real multi-step turn; pinned assembly executes once per turn
    - _Requirements: 3.1, 3.2, 3.3_

## P0 — Mechanism cores: ledger, predictions, graph

- [x] 2. The epistemic state structures and their loop wiring
  - [x] 2.1 Premise ledger: extraction, provenance, resident rendering
    - Implement the ledger as agent-core run state: premise extraction at plan formation and plan change (deterministic + cheap-model hybrid), provenance cited(source,citation)|assumption, transitions (discharge, refute)
    - Render the ledger resident in the fixed tail slot, updated the step a transition occurs; feed premise provenance into buildCassandraSignals as a first-class doubt input
    - Test: a real plan against the real loop produces a correctly-provenanced, resident-rendered ledger
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [x] 2.2 Prediction-carrying dispatch: expectations, mismatch detection, per-strategy meter
    - Add the probe-class classifier beside the dispatch seam and the expectation channel on tool calls; deterministic mismatch detection (HTTP status, exit code, empty/nonempty, parse) with cheap-model comparison fallback
    - Wire mismatch as a belief-update event: ledger update, structured guidance render, per-strategy meter accumulation across differently-argued probes
    - Test: expectations ride real dispatches; deterministic mismatches detected; the meter aggregates one strategy across varied args
    - _Requirements: 6.1, 6.2_
  - [x] 2.3 Reified task graph + todo projection
    - Implement the live task graph (goal → subgoals → premises → evidence) as agent-core run state, seeded from the plan, every dispatched action linked to a subgoal; render resident in the fixed tail slot
    - Make the todo tool a projection of the graph (one vocabulary, no parallel plan state)
    - Test: the graph tracks a real run; the todo projection stays consistent with graph state
    - _Requirements: 7.1, 7.3, 7.4_

## P1 — Enforcement: the gates that make assumption impossible

- [x] 3. Check-before-act, forced revision, measured convergence
  - [x] 3.1 Check-before-act gate + self-claim validation
    - Gate dispatch on the ledger: refuse actions depending on cheaply-checkable unverified assumptions with a directive naming premise + check; discharge/refute on check; refuted premises force plan revision before dependent dispatches
    - Deterministic self-claim validation against the resident capability surface (identity-scrub posture); contradictions surface as mismatch events
    - Test: refusal, discharge, refute→revision, and self-claim contradiction paths on the real loop + dispatch seams
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 3.2 Forced revision + measured convergence wired into the loop
    - Force the tools-stripped reasoning-only revision step at NEO_EPISTEMIC_MISMATCH_LIMIT consecutive per-strategy mismatches and at NEO_EPISTEMIC_CONVERGENCE_WINDOW actions without evidence growth; no-expectation probes refused with the ground-or-hypothesize directive
    - Keep NoProgressStall et al as outer failsafes that the epistemic layer fires before (guards-as-failsafes posture preserved)
    - Test: revision forced at both bounds on real non-converging sequences; failsafe ordering proven; guess-class refusal proven
    - _Requirements: 6.3, 6.4, 7.2, 7.5_
  - [x] 3.3 Epistemic config surface + clean disablement
    - Add env + kvx [epistemic] knobs (PREMISES, PREDICTIONS, MISMATCH_LIMIT, CONVERGENCE_WINDOW) with defaults on/on/3/4; disabling any mechanism cleanly restores prior behavior
    - Test: both states of each knob against the real loop
    - _Requirements: 8.1, 8.2_

## P2 — The corpus proofs

- [x] 4. Replay the two real failures against the new core
  - [x] 4.1 Arena replay: the self-premise dies at birth
    - **Property ARENA: the arena prompt from cold, against a real agent with a real self-model artifact carrying the true external surface, produces a plan whose self-referential premise is cited from the resident self-model BEFORE any scaffold action dispatches; no dispatched action depends on an unchecked cheaply-checkable assumption — real loop, real dispatch, real artifact, no fakes**
    - **Validates: Requirements 9.1, 9.3**
  - [x] 4.2 OHLC replay: guessing self-terminates by probe three
    - **Property OHLC: against a real httptest API with an undocumented path, every probe carries an expectation; by the third failed probe the core has forced a tools-stripped revision step; the mismatch meter and premise ledger reflect the failures; total probes bounded — real loop, real endpoints, no fakes**
    - **Validates: Requirements 9.2, 9.3**

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["1.3", "2.1", "2.2", "2.3"] },
    { "id": 3,  "tasks": ["3.1", "3.2", "3.3"] },
    { "id": 4,  "tasks": ["4.1", "4.2"] }
  ]
}
```
