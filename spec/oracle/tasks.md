<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: ORACLE — vault first (crypto core, stores, migration, deletion), then personalization (profile, schedule, brief) on the sealed substrate

## Tasks

## P0 — The vault core

- [ ] 1. Envelope crypto, keys, permissions
  - [x] 1.1 vault/ crypto core: envelope, header, AEAD shapes
    - New top-level module vault/: KEK KeyProvider seam (file/env dev provider + remote KMS provider shape), per-user wrapped UK mint/load/rotate, per-object DEK wrap/unwrap, AES-256-GCM primitives, versioned self-describing header (magic, format version, key id, wrapped DEK, AD schema version), canonical AD encoding
    - The three ciphertext shapes: record-AEAD (single line/value), whole-file AEAD, chunked streaming AEAD with per-chunk index+final AD framing
    - Tests: round-trips all shapes, wrong-key/tamper/truncation failures, header versioning, KEK rotation re-wraps UK without data rewrite, no raw key bytes in any error/serialized output
    - _Requirements: 1.1, 1.2, 1.3, 1.5_
  - [x] 1.2 Fail-closed wiring: VAULT_REQUIRED, provision, boot
    - Per-user UK provision/load on daemon boot (wrapped keyfile beside /data), VAULT_REQUIRED env (router MachineEnv injects true in prod): unavailable KeyProvider/UK = hard write error + boot refusal; explicit false = legacy plaintext for dev/CLI
    - Tests: boot refusal with vault required and no key, hard write error path, dev fallback only when explicitly disabled
    - _Requirements: 1.4_
  - [x] 1.3 Permission sweep: 0700 dirs, 0600 files
    - Sweep MkdirAll/OpenFile/WriteFile/Create modes across neo conversation/trace/task/media stores, construct surfacestore, executor async/transcript/conversation stores, automatrix settings+log, cortex store dir
    - Tests asserting modes on really-created dirs/files in each store
    - _Requirements: 6.1_

## P0 — Store encryption

- [ ] 2. Wire every persistence surface through the vault
  - [x] 2.1 Encrypted JSONL: conversations, traces, surfaces, transcripts
    - vault/jsonl encrypted reader/writer (record AEAD + AD binding user/store/conv-run/seq/schema; header-sniffing reads carry legacy plaintext); wire neo/internal/conversation (hot+archive), neo/internal/trace, construct/surfacestore, executor transcripts + daemon conversation threads
    - Tests per store on real files: encrypted append/read/trim/rollup, torn-tail crash recovery, AD cross-swap rejection, legacy-file readability
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [x] 2.2 Encrypted atomic JSON: tasks, async jobs, settings, envelopes
    - vault/file whole-file AEAD; wire neo/internal/task, executor asyncRegistry, conversation meta sidecars, automatrixsettings + automatrixlog, persisted intent envelopes; tmp+rename atomicity preserved
    - Tests: round-trip + tamper + wrong-user on real files, atomic replace under simulated crash
    - _Requirements: 3.1, 3.2_
  - [x] 2.3 Cortex value encryption below the hash boundary
    - Encrypt/decrypt at the cortex store value seam (WriteBatch.Set/Get + read paths) for j/, m/, mv/, sess/, sessblob/, roll/, enr/, story/, chk/, vec/meta/ values; leaf/SMT hashes stay over canonical plaintext CBOR; replay decrypts before re-hash
    - Tests: OverallRoot byte-identity plaintext vs encrypted and across replay-rebuild; recall/activate/rollup paths green over an encrypted store; documented residual-leakage note (keys, sizes)
    - _Requirements: 4.1, 4.2, 4.3_
  - [-] 2.4 Media + snapshot/backup streaming encryption
    - vault/stream wired into the media plane (uploads, generated media, screenshots) and the daemon snapshot push/pull (tarball encrypted before S3/MinIO, decrypt-verify on restore)
    - Tests: streamed round-trip, truncated/reordered chunk rejection, real snapshot backup/restore round-trip
    - _Requirements: 5.1, 5.2_

## P1 — Migration and the gate

- [ ] 3. Encrypt the existing fleet safely
  - [ ] 3.1 Per-user migrator: quiesce, encrypt, verify, swap
    - Migration tool: quiesce one user, encrypt each file into a temporary sibling, verify counts + authenticated round trips against the source, atomic rename over plaintext; cortex value-batch rewrite proving OverallRoot identity; never dual-write plaintext
    - Tests on a real seeded user tree: full migrate, crash-mid-migrate resume, post-migrate reads green
    - _Requirements: 7.1, 7.2, 7.3_
  - [ ] 3.2 Forbidden-plaintext scanner gate
    - vault scanner walking a data tree flagging plaintext content artifacts for migrated users; wired as boot check + CI gate
    - Tests: clean tree passes, planted plaintext artifact fails
    - _Requirements: 6.2_
  - [ ] 3.3 Adversarial vault property suite
    - **Property VAULT: on real storage paths — wrong-user key fails decrypt; flipped bytes and cross-context (user/store/conversation/seq) record swaps fail authentication; truncated records/chunks detected or safely skipped per shape; crash mid-append leaves a readable store; KEK rotation re-wraps without data rewrite; snapshot restore round-trips; no fakes anywhere**
    - **Validates: Requirements 10.1, 10.2, 10.3**

## P1 — Deletion and consent

- [ ] 4. Cryptographic erasure and training lineage
  - [ ] 4.1 Deletion pipeline + receipt
    - Router-orchestrated deletion: disable personalization/training collection, cancel Chronos alarms + drain background jobs, destroy the wrapped UK, delete ciphertext from active /data, MinIO snapshot generations incl. old versions, and training staging per a documented retention schedule; durable per-stage receipt in router Postgres; minimal non-content compliance record
    - Tests: post-deletion ciphertext unreadable (UK gone), receipt records every stage, alarms cancelled, plaintext scanner clean
    - _Requirements: 8.1, 8.2, 8.3, 8.4_
  - [ ] 4.2 Training-consent lineage seam
    - Consent grant kept independent of personalization opt-in; export manifest contract (consent version/time, source record IDs, identity, batch id); revocation stops selection + triggers lineage deletion; dataset-scoped vault keys/roles disjoint from user UKs
    - Tests against real router Postgres: manifest completeness, revocation stops exports, lineage deletion reaches staged batches
    - _Requirements: 9.1, 9.2, 9.3_

## P2 — Personalization

- [ ] 5. Profile, interview, schedule, brief
  - [x] 5.1 Personalization profile record + endpoints
    - Dedicated versioned tagged pinned cortex record (tag personalization-profile; schema: interests, day-to-day goals, media tastes liked+disliked per category, adventurousness, brief preferences) — separate from onboarding profile; daemon GET/PUT/DELETE + export endpoints mirroring daemon_profile_routes.go; no sensitive-trait fields
    - Tests: real-cortex round-trip, versioned edit, delete removes the authoritative record, export shape
    - _Requirements: 13.1, 13.2, 13.3_
  - [x] 5.2 briefsettings + MORNING_BRIEF Chronos convention + governor + routes
    - chronos/internal/morningbrief (marker wake_message + BuildAlarm, cron at local time in IANA tz, idempotency key) sibling of automatrix; neo briefsettings durable sidecar (enabled, tz, time, days, length, sections, paused, alarm id, destination conversation, last-delivered date); governor (fail-closed enable = create exactly one alarm, disable/pause = cancel/skip, settings change = reschedule) via the MCP alarm controller; Neo-owned /brief/settings routes
    - Tests: alarm lifecycle idempotence, DST-correct next-fire via schedule.NextCron, live re-read on toggle, default off
    - _Requirements: 11.1, 11.3, 14.1, 14.2_
  - [x] 5.3 Guided interview + confirmation flow
    - Interview conversation flow in Neo: five skippable question groups one at a time, plain-language summary, explicit confirmation writes the single profile record; interview conversations excluded from writeback extraction (no fragmented inferred memories); repeat/edit re-enters with existing answers
    - Tests: real engine + httptest LLM — skip paths, confirmation-gated persistence, writeback exclusion, repeat-with-existing
    - _Requirements: 12.1, 12.2, 12.3_
  - [x] 5.4 The brief runner: restricted, supervised, durable
    - MORNING_BRIEF wake handler (re-read settings every fire, fail closed, day/date guards, never twice per local date); supervised run on a positive allowlist (web_news/web_search/fetch/memory recall; money/signing/fs/deploy/exec + core_execute structurally absent) via the advertised-set mechanism; durable through the task ledger with restart resume; brief persisted as conversation turn + inbox record before notification
    - Tests: structural exclusion via AdvertisedTools, zero network when disabled/paused, supervisor resume of an interrupted brief, single-delivery-per-date
    - _Requirements: 14.3, 15.1, 15.2, 15.3_
  - [x] 5.5 Brief composition policy + recommendation history/feedback
    - Composer policy: <=5 news items from fresh in-run retrieval with date+source link+why-relevant, 1-2 rotating recommendations, 'Neo can help today' from stated goals, facts-vs-recommendations distinction, disclosure rule; briefhistory durable sidecar (delivered rec ids + news URLs + feedback liked/not_interested/less_of_this) consulted at composition; feedback flows to the profile only as explicit user-attributed entries
    - Tests: policy post-check on composed briefs, no-repeat property across runs, feedback round-trip into history + profile
    - _Requirements: 16.1, 16.2, 16.3, 17.1, 17.2_
  - [x] 5.6 Client surfaces: settings, interview entry, brief delivery
    - apps/client: personalization section in Settings (enable/disable, start/repeat interview, time/timezone/days/length, sections, pause, view/export/delete profile), interview runs in the normal chat surface, brief renders as a normal conversation turn + inbox item; i18n x5 locales; house rules (no borders/emojis/gradients/glow)
    - Verify: tsc + eslint + vitest green on changed files
    - _Requirements: 11.2_

## P3 — End-to-end proof

- [ ] 6. The whole loop on real components
  - [ ] 6.1 Half-B property + E2E suite
    - **Property ORACLE-B: on the real engine — default-off + opt-in gating (a wake with the feature off dispatches zero tools), exactly-one-alarm idempotence, day/date guards, structural tool exclusion asserted against the REAL advertised schema set; E2E: interview -> confirm -> profile record -> wake -> supervised run over httptest-backed search/fetch -> durable source-backed brief + history, interrupted-brief supervisor resume, no-repeat across consecutive runs — no fakes anywhere**
    - **Validates: Requirements 18.1, 18.2**

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.3"] },
    { "id": 2,  "tasks": ["1.2"] },
    { "id": 3,  "tasks": ["2.1", "2.2"] },
    { "id": 4,  "tasks": ["2.3", "2.4"] },
    { "id": 5,  "tasks": ["3.1", "3.2", "3.3"] },
    { "id": 6,  "tasks": ["4.1", "4.2"] },
    { "id": 7,  "tasks": ["5.1", "5.2"] },
    { "id": 8,  "tasks": ["5.3", "5.4", "5.5"] },
    { "id": 9,  "tasks": ["5.6"] },
    { "id": 10, "tasks": ["6.1"] }
  ]
}
```
