<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: Neo Execution Reliability

## Tasks

## P1 — Shared error taxonomy (Cluster A foundation)

- [ ] 1. A single shared failure classifier across the seam
  - [ ] 1.1 Define the failure classification (transient | deterministic | conflict | pending) over delegate/daemon outcomes
    - Add a classifier in neo/internal/delegate that maps an HTTP status + structured body to a typed class; 409 already-exists -> conflict, 403 leash/mode/cap -> deterministic, 422 clarify -> pending, 5xx/timeout/conn-refused -> transient, validation/unparseable -> deterministic
    - Carry the class on the error returned to neo/internal/agent so dispatchWithRetry and the supervisor read the SAME classification (no per-layer re-guessing)
    - _Requirements: 1.1, 1.4_
  - [ ] 1.2 Short-circuit deterministic tool failures in the dispatch ladder
    - In dispatchWithRetry/dispatchCoreExecute, when the class is deterministic, return a single terminal result with NO further attempts (mirror chatWithRetry's ErrProviderRejected short-circuit)
    - Keep bounded backoff for transient; leave conflict/pending to their dedicated handlers (tasks 2.x / 4.x)
    - _Requirements: 1.2, 1.3, 1.5, 3.1_
  - [ ] 1.3 Teach the supervisor to not respawn deterministic failures
    - In session.go superviseDecision, branch on the failure class: deterministic -> stop-and-ask / honest-partial surface (no respawn, do not consume the respawn budget); transient/model/stall -> existing respawn path
    - Replace the generic 'still on it, taking another run at it' copy for the deterministic-stop case with a plain statement of what blocked the task + the user's next step
    - _Requirements: 3.2, 3.3, 3.4_

## P2 — Idempotent core_execute (Cluster A core, the SPARK fix)

- [ ] 2. Attach-to-existing on conflict
  - [ ] 2.1 Delegate attach-to-existing on a 409 conflict
    - On conflict, GET the existing intent's async status + /intents/{id}/gates instead of failing; if in-flight, attach: poll progress and service its gate through the SAME approver/notify wiring the create path uses
    - If the conflicting intent is already terminal, return its outcome in plain language; never re-submit fresh prose
    - _Requirements: 2.1, 2.2, 2.3, 2.5_
  - [ ] 2.2 Make attach idempotent + concurrency-safe
    - Guard gate answering and result counting so two attaches to the same intent cannot double-answer a gate or double-count an outcome (keyed on intent id + node id)
    - Decide + document the daemon-side option (idempotent 200+existing-id create) vs delegate-side reconcile; implement the delegate-side reconcile first as it does not touch the signed path
    - _Requirements: 2.4_

## P3 — Loop + batch safety (Clusters A + C)

- [ ] 3. Stop the self-inflicted loops
  - [ ] 3.1 Semantic no-progress detection
    - Augment the no-progress guard to key on (tool + normalized target/operation) so reworded args at the same operation count as a repeat; trip a stall after N deterministic repeats (configurable)
    - Guard against false-trips on genuinely distinct operations sharing a tool (different targets do not count as repeats)
    - _Requirements: 4.1, 4.2, 4.3_
  - [ ] 3.2 Batch idempotency for non-idempotent tool calls
    - In runToolCalls, dedup/serialize state-touching calls (core_execute) within a batch: collapse equivalent calls (same resolved intent id) to one run + join the other to its result; keep concurrent dispatch for independent reversible calls
    - _Requirements: 5.1, 5.2, 5.3_

## P4 — Transparency (Cluster B)

- [ ] 4. Plain-language results, guarded thinking, coalesced narration
  - [ ] 4.1 Jargon-free tool-result translation at the delegate boundary
    - Map every daemon outcome class to a plain-language tool result from a fixed phrase set (mirror the daemon's failureMessage taxonomy); never interpolate raw bodies/codes/intent-ids or any jargon-deny term
    - Preserve the failure CLASS for the agent (it still learns it failed + why, in clean terms) while stripping internals from the user-facing surface
    - _Requirements: 6.1, 6.3, 6.4_
  - [ ] 4.2 Reasoning-channel jargon guard
    - Scrub/guard chat.delta channel=reasoning against the jargon-deny set before it streams to the Thinking panel (server emit), so inferred internals never surface
    - _Requirements: 6.2_
  - [ ] 4.3 Narration coalescing (server + client)
    - Coalesce identical consecutive narration on the same run in engine.notifyFor (collapse into the existing bubble, optional repeat indicator); emit the 'I'll ask before anything spends' promise only after a submission is accepted/attached
    - Defend in the client reducer (useChat.ts): collapse duplicate consecutive assistant text even across differing seqs
    - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - [ ] 4.4 Structured clarify pass-through with slot-fill
    - Carry the daemon's 422 slot schema back to Neo as an actionable clarify (slot_name/type_name/prompt); on user answers, re-POST the SAME intent id with slot_values; surface plainly + stop if unsatisfiable
    - _Requirements: 8.1, 8.2, 8.3_

## P5 — Memory consistency (Cluster D)

- [ ] 5. Clean write-path, supervised pinning, cheaper consistent reads
  - [ ] 5.1 Dedup-first write path + fail-closed-to-skip classify
    - In the writeback consolidator / pager Remember*Related, run a deterministic embedding-cosine near-duplicate check over same-type neighbors BEFORE the LLM classify; skip writing a highly-similar memory
    - When the classify model errors AND the candidate is highly similar, skip/supersede instead of defaulting to RelationNew (a new record)
    - _Requirements: 9.1, 9.2_
  - [ ] 5.2 Supervised pinning of user_facts + corrections
    - Require a confidence threshold + provenance before a user_fact or correction (both pinned every turn) is written; decay/retire standing corrections that go unused
    - _Requirements: 9.3, 9.5_
  - [ ] 5.3 User-visible, editable 'what Neo learned about you' surface
    - Expose the pinned user facts + standing corrections to the user and allow removal (uses the existing client surface conventions; no new signing path)
    - _Requirements: 9.4_
  - [ ] 5.4 Per-turn pinned compute + consistent learned-guidance
    - Hoist pager.Pinned out of the per-step loop (compute once per turn, or cache + invalidate on cortex write); pinned CONTENT unchanged
    - Make standing corrections always-pinned (capacity-managed by consolidation) vs salience-ranked soft preferences; consolidate overlapping guidance lines so near-duplicates don't crowd the budget
    - _Requirements: 10.1, 10.2, 10.3, 10.4_

## P6 — Verification (no fakes)

- [ ] 6. Prove it against real failure modes
  - [ ] 6.1 Error classifier against real daemon responses
    - Drive a real MCL daemon to emit a 409 conflict, a 403 leash denial, a 422 clarify, and a 5xx; assert the classifier maps each to the correct class and the ladder/supervisor act correctly (no retry/respawn on deterministic)
    - **Property 1: real-response classification + correct retry/stop/attach decision**
    - **Validates: Requirements 1.1, 1.2, 1.3, 3.1, 3.2**
  - [ ] 6.2 Attach-to-existing end-to-end (the SPARK reproduction)
    - Against a real daemon async registry: submit an intent that parks at a gate, then issue a SECOND equivalent core_execute; assert it ATTACHES (no second submission) and surfaces the SAME pending gate.invoked
    - **Property 2: a re-dispatched in-flight launch joins + shows the existing approval, never loops**
    - **Validates: Requirements 2.1, 2.2, 2.3, 2.4, 5.1**
  - [ ] 6.3 Transparency assertions on the SPARK transcript
    - Replay the looping-launch transcript through the seam; assert the user-visible stream (bubbles + Thinking) contains NO jargon-deny term and NO duplicate consecutive narration bubble
    - **Property 3: no jargon leak, no narration spam on the failure path**
    - **Validates: Requirements 6.1, 6.2, 7.1, 7.2**
  - [ ] 6.4 Memory dedup + per-turn pinned compute against real cortex
    - Against the real cortex store + pager: assert no near-duplicate writes under a deliberately-flaky classify model, and assert a multi-step turn issues materially fewer pinned-block cortex scans than the per-step baseline
    - **Property 4: clean memory writes + reduced read cost, content unchanged**
    - **Validates: Requirements 9.1, 9.2, 10.1, 10.4**
  - [ ] 6.5 Non-perturbation check (signed walk + gate UI untouched)
    - Assert the signed MCL path journaled bytes + D11 replay roots are byte-identical before/after this feature, and that gate.invoked/gate.decided + wallet-approval.tsx are unchanged (only REACHED via attach)
    - **Property 5: D11 byte-identity + gate contract preserved**
    - **Validates: Requirements 11.1, 11.2, 11.3, 11.4**

- [ ] 7. Final checkpoint — Neo execution-seam reliability complete
  - All five properties green; SPARK-style launch parks at its approval once (no loop, no jargon, no spam); memory stays clean; the signed MCL walk and the gate UI are provably untouched. Deploy is user-driven (Andrew drives prod redeploys). Ask the user if questions arise.

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1"] },
    { "id": 2,  "tasks": ["1.2", "1.3"] },
    { "id": 3,  "tasks": ["2.1", "3.1", "3.2"] },
    { "id": 4,  "tasks": ["2.2", "4.1", "5.1", "5.4"] },
    { "id": 5,  "tasks": ["4.2", "4.3", "4.4", "5.2"] },
    { "id": 6,  "tasks": ["5.3", "6.1"] },
    { "id": 7,  "tasks": ["6.2", "6.3", "6.4", "6.5"] }
  ]
}
```
