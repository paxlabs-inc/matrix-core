<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: NEOCORTEX — scaffold the engine and its adversarial CI, prove the log and the deterministic core, gate beliefs behind evidence, build the deterministic-first index plane, make INV-1 machine-checked, expose cortexd and the Go seam, then migrate and cut over under owner gates

## Tasks

## W1 — Engine skeleton, toolchain lock, log, and tamper evidence

- [x] 1. Repository scaffold, frame codec, segment log, MMR
  - [x] 1.1 neocortex module scaffold with locked toolchain and adversarial CI
    - Create the neocortex/ module exactly per schematics.md section 2: CMake build with the pinned toolchain and flags from agent.lock.kvx, vendored digest-pinned dependencies, hardened libc++, -fno-exceptions core, clang-tidy ruleset, and cross-compilation for amd64/arm64.
    - Stand up CI jobs: build matrix, ASan/UBSan/TSan suites, libFuzzer smoke targets, clang-tidy zero-warning gate, and a reproducible-build check.
    - Prove the scaffold with a real end-to-end walking skeleton: open an actor directory, append one raw frame, read it back, verify checksum — under ASan and the fuzzer harness.
    - _Requirements: 8.1, 8.2, 8.4_
  - [x] 1.2 Segment log: frame codec, group commit, torn-tail recovery
    - Implement the frame format and segment files from schematics.md section 3: length/crc32c/lsn/kind/timestamp/actor/conversation header, sealed payload slot, bounded segments, io_uring write path with a portable pwrite fallback, group-commit durability barriers.
    - Implement boot verification: tail checksum walk, single torn-frame truncation at the physical tail, fail-closed interior-corruption refusal with exact lsn reporting.
    - Test with real files and injected faults: torn writes, short reads, crash between frames; fuzz the frame parser; property-test append/read round-trips.
    - _Requirements: 1.1, 1.3, 8.5_
  - [x] 1.3 BLAKE3 MMR, signed checkpoints, and range verification
    - Implement the merkle mountain range over frame hashes with O(log n) append and range proofs, ed25519-signed checkpoint roots, and an offline verifier CLI.
    - Bind verification into boot and the admin surface so the MEMORY VERIFIED root line survives the rewrite.
    - Property-test MMR consistency across appends, restarts, and simulated corruption; prove a forged frame or reordered range fails verification.
    - _Requirements: 1.4_

## W2 — Deterministic core, projections substrate, and sealing

- [x] 2. State machine, DST harness, LMDB projections, vault AEAD
  - [x] 2.1 Deterministic single-writer core and the simulation harness
    - Implement the single-writer apply loop with injected clock/entropy/IO interfaces per schematics.md section 5; no core path may touch wall time, randomness, or the filesystem directly.
    - Build the deterministic simulation harness: simulated disk with torn-write/short-read/fsync-lie fault models, simulated clock, scripted event workloads, and kill-at-any-lsn process death.
    - Prove replay convergence: identical logs yield byte-identical projection state across thousands of randomized fault schedules in CI.
    - _Requirements: 2.1, 2.4_
  - [x] 2.2 LMDB projection substrate and the rebuild law
    - Stand up per-actor LMDB environments with per-projection applied-lsn checkpoints, epoch-pinned read snapshots, and the writer/reader thread model from schematics.md section 5.
    - Implement the rebuild law as an operation: drop any or all projections, replay from the log, converge byte-identically; wire it to the admin rebuild trigger.
    - Property-test rebuild equivalence under the simulation harness, including mid-rebuild crash and resume.
    - _Requirements: 2.2, 2.3, 7.6_
  - [x] 2.3 Vault sealing below the hash boundary and crypto-deletion
    - Implement XChaCha20-Poly1305 record AEAD for frame payloads under the KEK to UK to DEK hierarchy with associated data binding actor/lsn/kind; hashes commit to plaintext so MMR verification is unchanged by sealing.
    - Implement key destruction as crypto-deletion with a verifiable receipt: post-destruction reads fail closed with a typed error.
    - Test round-trips, wrong-user rejection, tamper rejection, legacy-plaintext refusal, and AEAD property tests; fuzz the unseal path.
    - _Requirements: 1.5_

## W3 — Event schema, belief store, and the write gate

- [x] 3. Frozen taxonomy, typed upserts, contradiction and poisoning defenses
  - [x] 3.1 FlatBuffers event schema and the frozen kind taxonomy
    - Author the FlatBuffers schema for every event kind frozen in agent.lock.kvx, including exact provider-byte frames and the intent kinds; encode the write-ahead ordering rules (tool_call before execution, delivered_msg as the only delivery representation) into the apply-time validators.
    - Implement schema evolution rules (additive-only, verifier-before-trust) and reject unknown or forbidden kinds at both the socket and import boundaries.
    - Fuzz the schema verifier; property-test that no representable event encodes guidance/steering; golden-vector the canonical encodings.
    - _Requirements: 1.2, 1.6, 8.5_
  - [x] 3.2 Belief store projection: typed upserts, supersession, bi-temporality
    - Implement the belief store per schematics.md section 6: canonical-identity typed upsert, supersession chains, bi-temporal heads with as_of reads, and schema-mandatory provenance lsn ranges.
    - Implement retraction (typed retract events) and tombstone semantics honored by every projection and read path.
    - Property-test upsert idempotency, supersession ordering under replay, as_of correctness, and the unrepresentability of a provenance-free belief.
    - _Requirements: 3.2, 3.3, 3.6_
  - [x] 3.3 The write gate: contradiction edges and anti-poisoning rules
    - Implement apply-time gate semantics: same-identity assertions supersede; cross-identity contradictions record typed conflict edges; the composer contract marks conflicted beliefs as obligated surfacing.
    - Enforce the negative-existence rule: reject gone/defunct/nonexistent assertions lacking corroborating tool_result provenance, with a typed rejection the client can surface.
    - Test with real assertion streams reproducing the Moltbook poisoning and duplicate-fact incidents; prove both classes are structurally rejected or reconciled.
    - _Requirements: 3.1, 3.4, 3.5_

## W4 — Deterministic-first index plane

- [x] 4. Entity lane, exact SIMD vectors, BM25, temporal ladder
  - [x] 4.1 Entity index with guaranteed recall
    - Implement deterministic identifier extraction (domains, URLs, paths, hex and structured ids, proper names) at apply time into an exact-match entity table over roaring bitmaps, per schematics.md section 7.
    - Implement the query side: identifier and entity detection over activation queries and turn text with guaranteed surfacing of matching records.
    - Property-test extraction determinism and the recall guarantee: any stored identifier queried verbatim or by entity always surfaces; replay rebuilds the index byte-identically.
    - _Requirements: 4.1_
  - [x] 4.2 Exact SIMD vector lane and BM25 lexical lane
    - Implement the flat vector store (int8 quantization, binary prefilter, mmap-resident) with a highway-based SIMD exact scan; embeddings enter as embedding events so the lane rebuilds from the log alone.
    - Implement the BM25 inverted index over roaring bitmaps for messages and beliefs, and reciprocal rank fusion with a deterministic tiebreak across the two lanes.
    - Benchmark scan latency against the activation budget on realistic per-actor corpus sizes; property-test exactness against a scalar reference implementation; prove records without embeddings stay reachable via entity and lexical lanes.
    - _Requirements: 4.2, 4.3, 4.4_
  - [x] 4.3 Temporal ladder projection and recursive descent
    - Implement wall-clock windowed rollup records over the log with coarse-to-fine descent handles, rebuilt deterministically from event timestamps and lsns.
    - Expose descent as a read operation (list windows, open window, resolve members) preserving the RLM navigation pattern.
    - Property-test ladder idempotency and self-healing rebuild of missing or stale tiers.
    - _Requirements: 4.5_

## W5 — Continuity and the composer

- [x] 5. Intent frame, work ledger, one Activate, INV-1 proven at every lsn
  - [x] 5.1 Intent frame and work ledger projections
    - Implement the intent frame projection over intent_set/loop_opened/loop_closed with typed closure reasons and the write-ahead discipline; open loops persist in every activation until explicitly closed.
    - Implement the work ledger projection: dispatched/committed/returned/outcome-unknown effect states derived from write-ahead tool_call and tool_result events, with boot-time in-flight marking for reconciliation.
    - Test crash-at-uncertain-points scenarios in the simulation harness: in-flight effects surface as needing reconciliation, never as silently done or silently lost.
    - _Requirements: 5.2, 5.3, 1.6_
  - [x] 5.2 The activation composer
    - Implement Activate per schematics.md section 8: strict tier order (resident, intent frame, work ledger tail, conversation projection, entity hits, conflict-obligated beliefs, fused enrichment, descent handles), caller-supplied token model, per-tier spend and trim reporting, structured bundle with URIs and provenance.
    - Enforce the trim law: enrichment first, mid-conversation coarsening second, resident/intent/ledger-tail never; an oversized intent frame is a typed invariant violation.
    - Pin determinism with golden vectors (identical prefix, query, budget yield a byte-identical bundle) and wire surfaced-attestation return events.
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 5.5_
  - [x] 5.3 INV-1 machine-checked at every lsn
    - Script a representative multi-turn, multi-tool, multi-loop conversation workload in the simulation harness, including overflow pressure and compaction.
    - Kill the engine at every lsn, boot a successor, compose the next activation, and assert all INV-1 clauses: faithful conversation, reconciled ledger, complete intent frame, evidence-only briefing.
    - Run the property in CI as a hard gate; any clause failure at any lsn fails the build.
    - _Requirements: 5.1, 5.4, 5.6, 2.4_

## W6 — cortexd and the Matrix seam

- [x] 6. Process, protocol, Go client, crash isolation
  - [x] 6.1 cortexd process and the local protocol
    - Implement cortexd: Unix domain socket server with versioned FlatBuffers framing, request/response and subscription streams, per-connection capability tokens scoping actor namespaces, and idempotent append keyed by client sequence.
    - Implement the admin capability: health, latency histograms, log/projection stats, verification status, manual rebuild; no outbound network, no model calls, enforced structurally (no client library for either linked into the engine).
    - Fuzz the protocol framing; test capability isolation between actors; soak-test with concurrent clients under the sanitizer matrix.
    - _Requirements: 7.1, 7.2, 7.6, 8.5_
  - [x] 6.2 Go client implementing the resurrection loop seam
    - Create the Go client module implementing ActivationSource, TurnRecorder, EvidenceJournal, Consolidator sink, and CheckpointStore over the protocol, including checkpoint events and the latest-checkpoint projection replacing the turnstate role.
    - Implement crash isolation: bounded queue then honest typed failure while cortexd is down, supervisor restart wiring, reconnect with no lost acknowledged writes and no duplicate unacknowledged application.
    - Test against the real cortexd binary over a real socket: the full resurrection loop interface contract, kill -9 of cortexd mid-turn with recovery, and a real neo runtime loop smoke on the new substrate behind the flag.
    - _Requirements: 7.3, 7.4, 7.5_

## W7 — Migration, absorption, and gated cutover

- [-] 7. Neo hard cutover, absorption, consumer re-point
  - [x] 7.1 Remove Cortex v1 from Neo completely
    - Remove matrix/cortex from every Neo source file, test, module requirement, configuration field, CLI flag, constructor, runtime branch, and proxy path. Remove the substrate selector and all silent fallback behavior.
    - Make Neo's memory contracts and implementations Neocortex-native. Startup requires the real cortexd socket and scoped token; loss or unavailability returns an honest typed Neocortex error without touching Cortex v1.
    - Add a structural CI gate proving the Neo source tree and complete module graph cannot reach matrix/cortex. Exercise the real Neo daemon against the real cortexd process, including unavailable-cortexd behavior.
    - _Requirements: 9.1, 9.2, 7.3, 7.4_
  - [-] 7.2 Neocortex-only Neo qualification
    - Run the fixed-incident regression corpus, the INV-1 property, and the Moltbook/duplicate/poisoning reproductions with Neo built and run only against Neocortex.
    - Run real conversations on a dev daemon against cortexd, including process loss/restart, and prove no Neo process or build artifact opens a Cortex-v1 store.
    - Record exact green checks and any divergences; a behavioral divergence is a stop, not a footnote.
    - _Requirements: 9.1, 9.2, 9.3_
  - [ ] 7.3 Consumer re-point and the owner cutover gate
    - Re-point external consumers to cortexd clients: executor daemon cortex routes, bridge, MCP tooling; derive the MCL compile-cache seed from the neocortex checkpoint root under an owner-approved rule.
    - Assemble the consumer re-point evidence package: qualification results, INV-1 CI status, performance envelope, and an honest Neocortex failure/recovery plan with no Cortex-v1 fallback.
    - Keep unrelated-consumer migration, production restart/deployment, and deletion of Cortex-v1 data as explicit owner gates.
    - _Requirements: 9.4, 9.5_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2", "1.3"] },
    { "id": 2,  "tasks": ["2.1", "2.2", "2.3"] },
    { "id": 3,  "tasks": ["3.1", "3.2", "3.3"] },
    { "id": 4,  "tasks": ["4.1", "4.2", "4.3"] },
    { "id": 5,  "tasks": ["5.1", "5.2", "5.3"] },
    { "id": 6,  "tasks": ["6.1", "6.2"] },
    { "id": 7,  "tasks": ["7.1", "7.2", "7.3"] }
  ]
}
```
