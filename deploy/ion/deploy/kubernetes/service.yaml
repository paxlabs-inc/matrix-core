apiVersion: v1
kind: Service
metadata:
  name: ion
  namespace: ion
  labels:
    app.kubernetes.io/name: ion
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ion
  ports:
    - name: http
      port: 80
      targetPort: 8080
